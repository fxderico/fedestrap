<base:WpfUiWindow x:Class="Fedestrap.UI.Elements.About.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:base="clr-namespace:Fedestrap.UI.Elements.Base" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:pages="clr-namespace:Fedestrap.UI.Elements.About.Pages" xmlns:resources="clr-namespace:Fedestrap.Resources" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" Title="{x:Static resources:Strings.About_Title}" Width="850" Height="500" MinWidth="850" BorderBrush="{DynamicResource SystemAccentBrush}" BorderThickness="2" ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica" WindowStartupLocation="CenterScreen" xmlns:about="clr-namespace:Fedestrap.UI.Elements.About" Background="{ui:ThemeResource ApplicationBackgroundBrush}">
  <Grid>
    <Panel.Background>
      <LinearGradientBrush StartPoint="1,1" EndPoint="0,0">
        <GradientStop Offset="0.00" Color="{DynamicResource WindowBackgroundColorPrimary}" />
        <GradientStop Offset="0.70" Color="{DynamicResource WindowBackgroundColorSecondary}" />
        <GradientStop Offset="1.00" Color="{DynamicResource WindowBackgroundColorThird}" />
      </LinearGradientBrush>
    </Panel.Background>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <ui:TitleBar Name="RootTitleBar" Grid.Row="0" Padding="8" Title="{x:Static resources:Strings.About_Title}" Icon="pack://application:,,,/Fedestrap.png" ForceShutdown="False" MinimizeToTray="False" UseSnapLayout="True" />
    <Grid Name="RootGrid" Row="1" Margin="12,12,0,0">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <ui:NavigationStore Name="RootNavigation" Grid.Row="1" Grid.Column="0" Margin="0,0,12,0" SelectedPageIndex="0" Frame="{Binding ElementName=RootFrame}">
        <ui:NavigationBase.Items>
          <ui:NavigationItem Margin="0,0,0,12" Content="{x:Static resources:Strings.Menu_About_Title}" Icon="QuestionCircle48" Tag="about" PageType="{x:Type pages:AboutPage}" />
          <ui:NavigationItem Margin="0,0,0,12" Content="Supporters" Icon="Heart24" Tag="supporters" PageType="{x:Type pages:SupportersPage}" />
          <ui:NavigationItem Margin="0,0,0,12" Icon="Translate24" Tag="translators" PageType="{x:Type pages:TranslatorsPage}">
            <TextBlock Name="TranslatorsText" FontSize="10" Text="{x:Static resources:Strings.About_Translators_Title}" />
          </ui:NavigationItem>
          <ui:NavigationItem Margin="0,0,0,12" Content="{x:Static resources:Strings.About_Licenses_Title}" Icon="Code24" Tag="licenses" PageType="{x:Type pages:LicensesPage}" />
        </ui:NavigationBase.Items>
      </ui:NavigationStore>
      <Frame Name="RootFrame" Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" BorderBrush="{DynamicResource SystemAccentBrush}" Navigated="RootFrame_Navigated" />
    </Grid>
  </Grid>
</base:WpfUiWindow>