<base:WpfUiWindow x:Class="Fedestrap.UI.Elements.ContextMenu.ServerInformation" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:Fedestrap.UI.Elements.ContextMenu" xmlns:base="clr-namespace:Fedestrap.UI.Elements.Base" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" xmlns:models="clr-namespace:Fedestrap.UI.ViewModels.ContextMenu" xmlns:resources="clr-namespace:Fedestrap.Resources" Title="{x:Static resources:Strings.ContextMenu_ServerInformation_Title}" MinWidth="440" MinHeight="200" Width="460" SizeToContent="Height" ResizeMode="NoResize" ExtendsContentIntoTitleBar="True" WindowStartupLocation="CenterScreen" Background="{ui:ThemeResource ApplicationBackgroundBrush}">
  <Grid>
    <Panel.Background>
      <LinearGradientBrush StartPoint="1,1" EndPoint="0,0">
        <GradientStop Offset="0.00" Color="{DynamicResource WindowBackgroundColorPrimary}" />
        <GradientStop Offset="0.70" Color="{DynamicResource WindowBackgroundColorSecondary}" />
        <GradientStop Offset="1.1" Color="{DynamicResource WindowBackgroundColorThird}" />
      </LinearGradientBrush>
    </Panel.Background>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <ui:TitleBar Name="RootTitleBar" Grid.Row="0" Padding="8" Title="{x:Static resources:Strings.ContextMenu_ServerInformation_Title}" ShowMinimize="False" ShowMaximize="False" CanMaximize="False" KeyboardNavigation.TabNavigation="None" Icon="pack://application:,,,/Fedestrap.png" />
    <Grid Row="1" Margin="20,8,20,20">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,4,0,18">
        <Grid Width="180" Height="158" HorizontalAlignment="Center">
          <Border CornerRadius="8" Background="{DynamicResource ControlFillColorSecondaryBrush}" />
          <Border CornerRadius="8">
            <Border.Background>
              <ImageBrush Stretch="UniformToFill" ImageSource="{Binding GameIcon}" />
            </Border.Background>
          </Border>
          <Border CornerRadius="8" BorderThickness="1" BorderBrush="{DynamicResource ControlElevationBorderBrush}" />
        </Grid>
        <TextBlock FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" TextAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="380" Margin="0,12,0,0" Text="{Binding GameName}" />
        <TextBlock FontSize="13" HorizontalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,2,0,4" Text="{Binding Username, Mode=OneWay}" />
        <Border Margin="0,0,0,0" Padding="14,6" CornerRadius="8" HorizontalAlignment="Center" Background="{DynamicResource ControlFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{DynamicResource ControlElevationBorderBrush}">
          <StackPanel Orientation="Horizontal">
            <ui:SymbolIcon Symbol="People24" FontSize="16" VerticalAlignment="Center" />
            <TextBlock Text="Players:" VerticalAlignment="Center" Margin="8,0,5,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Text="{Binding PlayerCount, Mode=OneWay}" />
          </StackPanel>
        </Border>
      </StackPanel>
      <Border Grid.Row="1" Padding="16,14">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <TextBlock Grid.Row="0" Grid.Column="0" Text="Type" Margin="0,0,16,10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
          <TextBlock Grid.Row="0" Grid.Column="1" Margin="0,0,0,10" HorizontalAlignment="Right" TextAlignment="Right" Text="{Binding ServerType, Mode=OneWay}" />
          <TextBlock Grid.Row="1" Grid.Column="0" Text="Location" Margin="0,0,16,10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Visibility="{Binding ServerLocationVisibility}" />
          <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" HorizontalAlignment="Right" TextAlignment="Right" TextTrimming="CharacterEllipsis" Text="{Binding ServerLocation}" Visibility="{Binding ServerLocationVisibility}" />
          <TextBlock Grid.Row="2" Grid.Column="0" Text="Instance ID" Margin="0,0,16,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
          <TextBlock Grid.Row="2" Grid.Column="1" HorizontalAlignment="Right" TextAlignment="Right" TextTrimming="CharacterEllipsis" Text="{Binding InstanceId, Mode=OneWay}" />
          <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,12,0,0" Visibility="{Binding FriendsVisibility}">
            <TextBlock Text="Friends in this server" Margin="0,0,0,4" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <TextBlock TextWrapping="Wrap" Text="{Binding FriendsInServer, Mode=OneWay}" />
          </StackPanel>
        </Grid>
      </Border>
    </Grid>
    <Border Grid.Row="2" Padding="15" Background="{DynamicResource PrimaryBackgroundColor}">
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button MinWidth="100" Width="148" Content="{x:Static resources:Strings.ContextMenu_ServerInformation_CopyInstanceId}" ToolTip="Copy the Instance ID" AutomationProperties.Name="Copy Instance ID Button" Command="{Binding CopyInstanceIdCommand, Mode=OneTime}" />
        <Button MinWidth="100" Margin="12,0,0,0" Content="{x:Static resources:Strings.Common_Close}" IsCancel="True" ToolTip="Close this window" AutomationProperties.Name="Close Button" />
      </StackPanel>
    </Border>
  </Grid>
</base:WpfUiWindow>