<base:WpfUiWindow x:Class="Fedestrap.UI.Elements.ContextMenu.CustomThemeEditor" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit" xmlns:base="clr-namespace:Fedestrap.UI.Elements.Base" xmlns:controls="clr-namespace:Fedestrap.UI.Elements.Controls" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" Width="1080" Height="720" MinWidth="820" MinHeight="560" ExtendsContentIntoTitleBar="True" WindowStartupLocation="CenterScreen" Background="{ui:ThemeResource ApplicationBackgroundBrush}">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Panel.Background>
      <LinearGradientBrush StartPoint="1,1" EndPoint="0,0">
        <GradientStop Offset="0.00" Color="{DynamicResource WindowBackgroundColorPrimary}" />
        <GradientStop Offset="0.70" Color="{DynamicResource WindowBackgroundColorSecondary}" />
        <GradientStop Offset="1.00" Color="{DynamicResource WindowBackgroundColorThird}" />
      </LinearGradientBrush>
    </Panel.Background>

    <ui:TitleBar Name="RootTitleBar" Grid.Row="0" Title="Theme Editor" Padding="8" Icon="pack://application:,,,/Fedestrap.png" MinimizeToTray="False" ShowMaximize="True" />

    <Grid Grid.Row="1" Margin="16,6,16,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Name="PreviewColumn" Width="300" />
        <ColumnDefinition Name="PreviewGap" Width="14" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <Border Name="PreviewPane" Grid.Column="0" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{DynamicResource ControlElevationBorderBrush}">
        <Border.Background>
          <LinearGradientBrush StartPoint="1,1" EndPoint="0,0">
            <GradientStop Offset="0.0" Color="{DynamicResource WindowBackgroundColorPrimary}" />
            <GradientStop Offset="0.5" Color="{DynamicResource WindowBackgroundColorSecondary}" />
            <GradientStop Offset="1.0" Color="{DynamicResource WindowBackgroundColorThird}" />
          </LinearGradientBrush>
        </Border.Background>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <StackPanel>
            <TextBlock Text="Preview" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <Border Margin="0,10,0,0" CornerRadius="8" Padding="14" Background="{DynamicResource PrimaryBackgroundColor}" BorderThickness="1" BorderBrush="{DynamicResource ControlElevationBorderBrush}">
              <StackPanel>
                <TextBlock Text="Panel surface" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <TextBlock Margin="0,2,0,12" FontSize="12" Text="Secondary text sits on the panel" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                <ui:Button Content="Accent button" Appearance="Primary" HorizontalAlignment="Left" />
                <ui:ToggleSwitch Margin="0,12,0,0" IsChecked="True" Content="A toggle" />
                <ComboBox Margin="0,12,0,0" SelectedIndex="0">
                  <ComboBoxItem Content="A dropdown" />
                  <ComboBoxItem Content="Second item" />
                </ComboBox>
                <Border Margin="0,12,0,0" CornerRadius="6" Padding="8" Background="{DynamicResource NewTextEditorBackground}">
                  <StackPanel>
                    <TextBlock FontFamily="Consolas" FontSize="12" Text="editor text" Foreground="{DynamicResource NewTextEditorForeground}" />
                    <TextBlock FontFamily="Consolas" FontSize="12" Text="a link" Foreground="{DynamicResource NewTextEditorLink}" />
                  </StackPanel>
                </Border>
              </StackPanel>
            </Border>
          </StackPanel>
        </ScrollViewer>
      </Border>

      <TabControl Name="EditorTabs" Grid.Column="2" Background="#00FFFFFF" BorderThickness="0" SelectionChanged="EditorTabs_SelectionChanged">
        <TabItem Name="PresetsTab">
          <TabItem.Header>
            <StackPanel Orientation="Horizontal">
              <ui:SymbolIcon Symbol="Color24" Margin="0,0,6,0" />
              <TextBlock Text="Colours" />
            </StackPanel>
          </TabItem.Header>
          <Border Margin="0,10,0,0" CornerRadius="8" Background="{DynamicResource ControlFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{DynamicResource ControlElevationBorderBrush}">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="14,12">
              <ItemsControl Name="ColorList">
                <ItemsControl.GroupStyle>
                  <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                      <DataTemplate>
                        <TextBlock Margin="0,10,0,6" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="{Binding Name}" />
                      </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                  </GroupStyle>
                </ItemsControl.GroupStyle>
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Grid Margin="0,4">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="38" />
                      </Grid.ColumnDefinitions>
                      <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock TextTrimming="CharacterEllipsis" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Text="{Binding Label}" />
                      </StackPanel>
                      <ui:TextBox Grid.Column="1" Margin="6,0" FontFamily="Consolas" Text="{Binding Hex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                      <Button Grid.Column="2" Width="32" Height="32" Padding="0" ToolTip="Pick a colour" Click="Swatch_Click">
                        <Border Width="22" Height="22" CornerRadius="4" BorderThickness="1" BorderBrush="#55FFFFFF" Background="{Binding Swatch}" />
                      </Button>
                    </Grid>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </ScrollViewer>
          </Border>
        </TabItem>

        <TabItem Name="CodeTab">
          <TabItem.Header>
            <StackPanel Orientation="Horizontal">
              <ui:SymbolIcon Symbol="Code24" Margin="0,0,6,0" />
              <TextBlock Text="Code" />
            </StackPanel>
          </TabItem.Header>
          <Grid Margin="0,10,0,0">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="0,0,0,8">
              <StackPanel Orientation="Horizontal">
                <ui:Button Content="Format" Appearance="Secondary" Margin="0,0,8,0" Click="Format_Click" />
                <ui:Button Content="Pick colour" Appearance="Secondary" Icon="Color24" Margin="0,0,8,0" Click="PickColor_Click" />
                <ui:Button Content="Copy" Appearance="Secondary" Icon="Copy16" Margin="0,0,8,0" Click="CopyCode_Click" />
                <ui:Button Name="ExternalButton" Appearance="Secondary" Icon="Open24" Content="Open Editor" ToolTip="Open the theme in another editor. Saving there updates this window." Click="OpenExternal_Click" />
              </StackPanel>
              <ui:ToggleSwitch Name="PreviewToggle" HorizontalAlignment="Right" VerticalAlignment="Center" Content="Preview" IsChecked="True" Checked="PreviewToggle_Changed" Unchecked="PreviewToggle_Changed" />
            </Grid>
            <Border Grid.Row="1" CornerRadius="6" BorderThickness="1" BorderBrush="{DynamicResource ControlElevationBorderBrush}" ClipToBounds="True">
              <avalonedit:TextEditor Name="CodeEditor" controls:CodeEditorBehavior.Language=".xaml" Padding="8,6" ShowLineNumbers="True" WordWrap="False" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" />
            </Border>
          </Grid>
        </TabItem>
      </TabControl>
    </Grid>

    <Border Name="StatusBorder" Grid.Row="2" Margin="16,10,16,0" Padding="12,8" CornerRadius="6" Visibility="Collapsed" BorderThickness="1">
      <StackPanel Orientation="Horizontal">
        <ui:SymbolIcon Name="StatusIcon" Symbol="Warning24" Margin="0,0,8,0" VerticalAlignment="Top" />
        <TextBlock Name="StatusText" TextWrapping="Wrap" VerticalAlignment="Center" />
      </StackPanel>
    </Border>

    <Border Grid.Row="3" Margin="0,10,0,0" Padding="16,10" Background="{DynamicResource PrimaryBackgroundColor}">
      <Grid>
        <ui:Button Content="Reset to default" Appearance="Secondary" HorizontalAlignment="Left" Click="Reset_Click" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
          <ui:Button Name="SaveButton" Content="Save and apply" Appearance="Primary" Margin="0,0,8,0" Click="Save_Click" />
          <ui:Button Content="Close" Appearance="Secondary" IsCancel="True" Click="Close_Click" />
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</base:WpfUiWindow>
