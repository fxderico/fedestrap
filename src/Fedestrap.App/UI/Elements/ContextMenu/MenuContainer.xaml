<base:WpfUiWindow x:Class="Fedestrap.UI.Elements.ContextMenu.MenuContainer" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" xmlns:local="clr-namespace:Fedestrap.UI.Elements.ContextMenu" xmlns:base="clr-namespace:Fedestrap.UI.Elements.Base" xmlns:resources="clr-namespace:Fedestrap.Resources" Loaded="Window_Loaded" Closed="Window_Closed" Title="ContextMenuContainer" Width="0" Height="0" MinWidth="0" MinHeight="0" Top="-100" Left="-100" ShowInTaskbar="False" WindowStyle="None" ResizeMode="NoResize" WindowStartupLocation="Manual">
  <FrameworkElement.ContextMenu>
    <ContextMenu MinWidth="248">
      <FrameworkElement.Resources>
        <ResourceDictionary>
          <Style x:Key="{x:Type Separator}" TargetType="{x:Type Separator}">
            <Setter Property="Margin" Value="6,4" />
            <Setter Property="UIElement.Opacity" Value="0.6" />
          </Style>
        </ResourceDictionary>
      </FrameworkElement.Resources>
      <MenuItem Name="CantSeeOverlaysMenuItem" Click="CantSeeOverlaysMenuItem_Click">
        <HeaderedItemsControl.Header>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="26" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:SymbolIcon Grid.Column="0" Symbol="EyeOff24" FontSize="18" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold" Text="Can't See Overlays?" />
          </Grid>
        </HeaderedItemsControl.Header>
      </MenuItem>
      <Separator />
      <MenuItem IsEnabled="False">
        <HeaderedItemsControl.Header>
          <Grid Margin="2,4,2,6">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Image Grid.Column="0" Width="30" Height="30" Source="pack://application:,,,/Fedestrap.png" RenderOptions.BitmapScalingMode="HighQuality" />
            <TextBlock Name="VersionTextBlock" Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" Text="Fedestrap v2.4.1" />
          </Grid>
        </HeaderedItemsControl.Header>
      </MenuItem>
      <MenuItem Name="CurrentGameMenuItem" IsEnabled="False" Visibility="Collapsed">
        <HeaderedItemsControl.Header>
          <Grid Margin="0,2">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="26" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Width="22" Height="22" CornerRadius="5" ClipToBounds="True" VerticalAlignment="Center">
              <Image Name="CurrentGameIcon" Width="22" Height="22" Stretch="UniformToFill" />
            </Border>
            <TextBlock Name="CurrentGameNameTextBlock" Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
          </Grid>
        </HeaderedItemsControl.Header>
      </MenuItem>
      <Separator />
      <MenuItem>
        <HeaderedItemsControl.Header>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="26" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:SymbolIcon Grid.Column="0" Symbol="Info20" FontSize="18" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" Text="Info" />
          </Grid>
        </HeaderedItemsControl.Header>
        <ItemsControl.Items>
          <StackPanel>
            <Expander Header="FFlags" IsExpanded="True" Background="#00FFFFFF">
              <TextBlock Name="FlagsTextBlock" Margin="4,2,0,2" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Flags: 0" />
            </Expander>
            <Expander Header="PlayTime" IsExpanded="True" Background="#00FFFFFF">
              <TextBlock Name="PlayTimeTextBlock" Margin="4,2,0,2" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="PlayTime: 00:00:00" />
            </Expander>
            <Expander x:Name="QuestExpander" Header="Quests" IsExpanded="True" Background="#00FFFFFF" Visibility="Collapsed">
              <ItemsControl x:Name="QuestProgressList" Margin="4,2,0,2">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <StackPanel Margin="0,0,0,6">
                      <TextBlock Text="{Binding Display}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="11" TextTrimming="CharacterEllipsis" />
                      <ProgressBar Height="10"
                                   Margin="0,3,8,0"
                                   Minimum="0"
                                   Maximum="100"
                                   BorderThickness="0"
                                   Background="#33FFFFFF"
                                   Foreground="#3BA55D"
                                   Value="{Binding Percent, Mode=OneWay}" />
                    </StackPanel>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </Expander>
            <Expander Header="Memory Details" IsExpanded="True" Background="#00FFFFFF">
              <StackPanel Margin="4,2,0,2">
                <TextBlock Name="MemoryTextBlock" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontWeight="SemiBold" Text="Roblox: 0 MB" />
              </StackPanel>
            </Expander>
          </StackPanel>
        </ItemsControl.Items>
      </MenuItem>
      <Separator />
      <MenuItem Name="ServerDetailsMenuItem" Visibility="Collapsed" Click="ServerDetailsMenuItem_Click">
        <HeaderedItemsControl.Header>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="26" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:SymbolIcon Grid.Column="0" Symbol="Info28" FontSize="18" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" Text="{x:Static resources:Strings.ContextMenu_ServerInformation_Title}" />
          </Grid>
        </HeaderedItemsControl.Header>
      </MenuItem>
      <MenuItem Name="JoinClosestServerMenuItem" Visibility="Collapsed" Click="JoinClosestServerMenuItem_Click">
        <HeaderedItemsControl.Header>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="26" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:SymbolIcon Grid.Column="0" Symbol="Rocket24" FontSize="18" VerticalAlignment="Center" />
            <TextBlock Name="JoinClosestServerTextBlock" Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" Text="Join Closest Server" />
          </Grid>
        </HeaderedItemsControl.Header>
      </MenuItem>
      <MenuItem Name="InviteDeeplinkMenuItem" Visibility="Collapsed" Click="InviteDeeplinkMenuItem_Click">
        <HeaderedItemsControl.Header>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="26" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:SymbolIcon Grid.Column="0" Symbol="ClipboardLink24" FontSize="18" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" Text="{x:Static resources:Strings.ContextMenu_CopyDeeplinkInvite}" />
          </Grid>
        </HeaderedItemsControl.Header>
      </MenuItem>
      <MenuItem Name="GamePassDetailsMenuItem" Visibility="Collapsed" Click="GamePassDetailsMenuItem_Click">
        <HeaderedItemsControl.Header>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="26" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:SymbolIcon Grid.Column="0" Symbol="Money24" FontSize="18" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" Text="GamePass Info" />
          </Grid>
        </HeaderedItemsControl.Header>
      </MenuItem>
      <MenuItem Name="GameHistoryMenuItem" Visibility="Collapsed" Click="JoinLastServerMenuItem_Click">
        <HeaderedItemsControl.Header>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="26" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:SymbolIcon Grid.Column="0" Symbol="History24" FontSize="18" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" Text="{x:Static resources:Strings.ContextMenu_GameHistory_Title}" />
          </Grid>
        </HeaderedItemsControl.Header>
      </MenuItem>
      <Separator />
      <MenuItem Name="MusicMenuItem" Visibility="Collapsed" Click="MusicPlayerMenuItem_Click">
        <HeaderedItemsControl.Header>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="26" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:SymbolIcon Grid.Column="0" Symbol="MusicNote224" FontSize="18" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" Text="Music Player" />
          </Grid>
        </HeaderedItemsControl.Header>
      </MenuItem>
      <MenuItem Name="OutputConsoleMenuItem" Visibility="Collapsed" Click="OutputConsoleMenuItem_Click">
        <HeaderedItemsControl.Header>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="26" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:SymbolIcon Grid.Column="0" Symbol="AnimalRabbit24" FontSize="18" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" Text="{x:Static resources:Strings.Menu_Context_PlayerLogs}" />
          </Grid>
        </HeaderedItemsControl.Header>
      </MenuItem>
      <MenuItem Name="ChatLogsMenuItem" Visibility="Collapsed" Click="ChatLogsMenuItemMenuItem_Click">
        <HeaderedItemsControl.Header>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="26" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:SymbolIcon Grid.Column="0" Symbol="Chat24" FontSize="18" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" Text="{x:Static resources:Strings.ContextMenu_MessageLogs_Title}" />
          </Grid>
        </HeaderedItemsControl.Header>
      </MenuItem>
      <MenuItem Name="LogTracerMenuItem" Visibility="Collapsed" Click="LogTracerMenuItem_Click">
        <HeaderedItemsControl.Header>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="26" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:SymbolIcon Grid.Column="0" Symbol="DocumentCatchUp20" FontSize="18" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" Text="{x:Static resources:Strings.Common_OpenLogFile}" />
          </Grid>
        </HeaderedItemsControl.Header>
      </MenuItem>
      <Separator />
      <MenuItem Name="RichPresenceMenuItem" Header="{x:Static resources:Strings.Common_DiscordRichPresence}" IsCheckable="True" IsChecked="True" Visibility="Collapsed" Click="RichPresenceMenuItem_Click" />
      <MenuItem Name="FrameGenParentMenuItem" Visibility="Collapsed">
        <HeaderedItemsControl.Header>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="26" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:SymbolIcon Grid.Column="0" Symbol="TopSpeed24" FontSize="18" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" Text="Frame Gen" />
          </Grid>
        </HeaderedItemsControl.Header>
        <MenuItem Name="FrameGenMenuItem" Header="Frame Generation" IsCheckable="True" Click="FrameGenMenuItem_Click" />
        <MenuItem Name="FrameGenOverlayMenuItem" Header="Generated Frames Overlay" IsCheckable="True" Click="FrameGenOverlayMenuItem_Click" />
        <MenuItem Name="FrameGenSplitMenuItem" Header="Split Screen Compare" IsCheckable="True" Click="FrameGenSplitMenuItem_Click" />
      </MenuItem>
      <MenuItem Name="BrightnessTrackerLog" Visibility="Collapsed">
        <HeaderedItemsControl.Header>
          <StackPanel Margin="0,2">
            <TextBlock Text="Brightness" FontWeight="SemiBold" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="48" />
              </Grid.ColumnDefinitions>
              <Slider Grid.Column="0" Minimum="0" Maximum="100" TickFrequency="5" IsSnapToTickEnabled="True" VerticalAlignment="Center" Focusable="True" Value="{Binding Brightness, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
              <TextBlock Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding BrightnessDisplay}" />
            </Grid>
          </StackPanel>
        </HeaderedItemsControl.Header>
      </MenuItem>
      <MenuItem Name="ColorsTrackerLog" Visibility="Collapsed">
        <HeaderedItemsControl.Header>
          <StackPanel Margin="0,2">
            <TextBlock Text="Colors" FontWeight="SemiBold" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <TextBlock Text="Saturation" FontSize="11" Margin="0,0,0,2" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
            <Grid Margin="0,0,0,6">
              <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="48" />
              </Grid.ColumnDefinitions>
              <Slider Grid.Column="0" Minimum="0" Maximum="200" TickFrequency="5" IsSnapToTickEnabled="True" VerticalAlignment="Center" Focusable="True" Value="{Binding Saturation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
              <TextBlock Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding SaturationDisplay}" />
            </Grid>
            <TextBlock Text="Contrast" FontSize="11" Margin="0,0,0,2" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
            <Grid Margin="0,0,0,6">
              <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="48" />
              </Grid.ColumnDefinitions>
              <Slider Grid.Column="0" Minimum="0" Maximum="200" TickFrequency="5" IsSnapToTickEnabled="True" VerticalAlignment="Center" Focusable="True" Value="{Binding Contrast, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
              <TextBlock Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding ContrastDisplay}" />
            </Grid>
            <TextBlock Text="Color Temperature" FontSize="11" Margin="0,0,0,2" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="48" />
              </Grid.ColumnDefinitions>
              <Slider Grid.Column="0" Minimum="-100" Maximum="100" TickFrequency="5" IsSnapToTickEnabled="True" VerticalAlignment="Center" Focusable="True" Value="{Binding ColorTemperature, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
              <TextBlock Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding ColorTemperatureDisplay}" />
            </Grid>
          </StackPanel>
        </HeaderedItemsControl.Header>
      </MenuItem>
      <Separator />
      <MenuItem Click="CloseRobloxMenuItem_Click">
        <HeaderedItemsControl.Header>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="26" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:SymbolIcon Grid.Column="0" Symbol="WindowHeaderHorizontalOff20" FontSize="18" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" Text="{x:Static resources:Strings.ContextMenu_CloseRoblox}" />
          </Grid>
        </HeaderedItemsControl.Header>
      </MenuItem>
    </ContextMenu>
  </FrameworkElement.ContextMenu>
</base:WpfUiWindow>
