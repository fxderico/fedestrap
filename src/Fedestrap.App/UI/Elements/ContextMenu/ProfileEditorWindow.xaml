<base:WpfUiWindow
    x:Class="Fedestrap.UI.Elements.ContextMenu.ProfileEditorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:Fedestrap.UI.Elements.Base"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Width="560"
    Height="740"
    MinWidth="480"
    MinHeight="560"
    Background="{ui:ThemeResource ApplicationBackgroundBrush}"
    ExtendsContentIntoTitleBar="True"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Grid.Row="0"
            Title="Edit Profile"
            Padding="8"
            Icon="pack://application:,,,/Fedestrap.png"
            MinimizeToTray="False"
            ShowMaximize="False" />

        <ScrollViewer Grid.Row="1" Margin="16,8,16,8" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="RootStack">

                <TextBlock Text="Display name" FontWeight="SemiBold" Margin="0,4,0,4"/>
                <ui:TextBox x:Name="DisplayNameBox" MaxLength="32" PlaceholderText="Shown instead of your username, optional"/>

                <TextBlock Text="Username" FontWeight="SemiBold" Margin="0,14,0,4"/>
                <ui:TextBox x:Name="UsernameBox" MaxLength="20" PlaceholderText="3-20 characters: letters, numbers, underscore"/>
                <TextBlock Text="Changing this changes how you sign in too - make sure you'll remember it." FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>

                <TextBlock Text="Status" FontWeight="SemiBold" Margin="0,14,0,4"/>
                <ui:TextBox x:Name="StatusBox" MaxLength="80" PlaceholderText="What are you up to?"/>

                <TextBlock Text="About" FontWeight="SemiBold" Margin="0,14,0,4"/>
                <ui:TextBox x:Name="AboutBox" MaxLength="1000" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="90" PlaceholderText="Tell people about yourself"/>

                <TextBlock Text="Profile gradient" FontWeight="SemiBold" Margin="0,14,0,4"/>
                <ComboBox x:Name="GradientCombo" Width="220" HorizontalAlignment="Left" Padding="10,5" SelectionChanged="GradientCombo_SelectionChanged"/>
                <StackPanel x:Name="CustomGradPanel" Orientation="Horizontal" Margin="0,8,0,0" Visibility="Collapsed">
                    <TextBlock Text="Angle" VerticalAlignment="Center" Width="46"/>
                    <ui:TextBox x:Name="GradAngle" Width="56" Text="135"/>
                    <ui:TextBox x:Name="GradC1" Width="92" Margin="10,0,0,0" PlaceholderText="#7c3aed"/>
                    <ui:TextBox x:Name="GradC2" Width="92" Margin="8,0,0,0" PlaceholderText="#4f46e5"/>
                </StackPanel>

                <TextBlock Text="Avatar border" FontWeight="SemiBold" Margin="0,14,0,4"/>
                <ComboBox x:Name="BorderCombo" Width="220" HorizontalAlignment="Left" Padding="10,5" SelectionChanged="BorderCombo_SelectionChanged"/>
                <StackPanel x:Name="BorderSolidPanel" Orientation="Horizontal" Margin="0,8,0,0" Visibility="Collapsed">
                    <TextBlock Text="Color" VerticalAlignment="Center" Width="46"/>
                    <ui:TextBox x:Name="BorderSolid" Width="92" PlaceholderText="#ffffff"/>
                </StackPanel>
                <StackPanel x:Name="BorderGradPanel" Orientation="Horizontal" Margin="0,8,0,0" Visibility="Collapsed">
                    <TextBlock Text="Angle" VerticalAlignment="Center" Width="46"/>
                    <ui:TextBox x:Name="BorderAngle" Width="56" Text="135"/>
                    <ui:TextBox x:Name="BorderC1" Width="92" Margin="10,0,0,0" PlaceholderText="#7c3aed"/>
                    <ui:TextBox x:Name="BorderC2" Width="92" Margin="8,0,0,0" PlaceholderText="#4f46e5"/>
                </StackPanel>

                <TextBlock Text="Banner" FontWeight="SemiBold" Margin="0,14,0,4"/>
                <Border x:Name="BannerPreview" Height="96" CornerRadius="8" Background="#22000000" Margin="0,0,0,6"/>
                <StackPanel Orientation="Horizontal">
                    <ui:Button Content="Import image" Icon="Image24" Click="ImportBanner_Click"/>
                    <ui:Button Content="Remove" Appearance="Secondary" Margin="8,0,0,0" Click="RemoveBanner_Click"/>
                </StackPanel>

                <TextBlock Text="Links (up to 5)" FontWeight="SemiBold" Margin="0,14,0,4"/>
                <ItemsControl x:Name="LinksList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="140"/>
                                    <ColumnDefinition Width="6"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="6"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ui:TextBox Grid.Column="0" MaxLength="40" Text="{Binding Label, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Label"/>
                                <ui:TextBox Grid.Column="2" Text="{Binding Url, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="https://..."/>
                                <ui:Button Grid.Column="4" Icon="Delete24" Appearance="Secondary" Click="RemoveLink_Click"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <ui:Button x:Name="AddLinkBtn" Content="Add link" Icon="Add24" Appearance="Secondary" Margin="0,4,0,0" Click="AddLink_Click"/>

            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2" Margin="16,8,16,14">
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="Cancel" Appearance="Secondary" Click="Cancel_Click"/>
                <ui:Button x:Name="SaveBtn" Content="Save" Appearance="Primary" Margin="8,0,0,0" Click="Save_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</base:WpfUiWindow>
