<base:WpfUiWindow x:Class="Fedestrap.UI.Elements.Dialogs.PublishThemeDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:base="clr-namespace:Fedestrap.UI.Elements.Base" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" Width="520" SizeToContent="Height" ResizeMode="NoResize" ExtendsContentIntoTitleBar="True" WindowStartupLocation="CenterOwner" ShowInTaskbar="False" Background="{ui:ThemeResource ApplicationBackgroundBrush}">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Panel.Background>
      <LinearGradientBrush StartPoint="1,1" EndPoint="0,0">
        <GradientStop Offset="0.00" Color="{DynamicResource WindowBackgroundColorPrimary}" />
        <GradientStop Offset="0.70" Color="{DynamicResource WindowBackgroundColorSecondary}" />
        <GradientStop Offset="1.00" Color="{DynamicResource WindowBackgroundColorThird}" />
      </LinearGradientBrush>
    </Panel.Background>

    <ui:TitleBar Name="RootTitleBar" Grid.Row="0" Title="Publish theme" Padding="8" Icon="pack://application:,,,/Fedestrap.png" MinimizeToTray="False" ShowMaximize="False" ShowMinimize="False" />

    <StackPanel Grid.Row="1" Margin="20,10,20,0">
      <TextBlock Name="IntroText" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

      <TextBlock Text="Name" Margin="0,16,0,4" FontWeight="SemiBold" />
      <ui:TextBox Name="NameBox" MaxLength="60" PlaceholderText="My theme" />

      <TextBlock Text="Description" Margin="0,14,0,4" FontWeight="SemiBold" />
      <ui:TextBox Name="DescriptionBox" MaxLength="600" Height="88" AcceptsReturn="True" TextWrapping="Wrap" VerticalContentAlignment="Top" PlaceholderText="What does it look like, what inspired it" />

      <StackPanel Name="NotePanel" Visibility="Collapsed">
        <TextBlock Text="What changed" Margin="0,14,0,4" FontWeight="SemiBold" />
        <ui:TextBox Name="NoteBox" MaxLength="200" PlaceholderText="Rounded the corners and fixed the status text" />
      </StackPanel>

      <Border Name="WarningBorder" Margin="0,14,0,0" Padding="12,10" CornerRadius="6" Visibility="Collapsed" Background="{DynamicResource ControlFillColorDefaultBrush}">
        <TextBlock Name="WarningText" TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
      </Border>

      <TextBlock Name="StatusText" Margin="0,12,0,0" TextWrapping="Wrap" FontSize="12" Visibility="Collapsed" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
    </StackPanel>

    <Border Grid.Row="2" Margin="0,18,0,0" Padding="20,12" Background="{DynamicResource PrimaryBackgroundColor}">
      <Grid>
        <TextBlock Name="OpenPageText" VerticalAlignment="Center" FontSize="12" Visibility="Collapsed">
          <Hyperlink Name="OpenPageLink" Click="OpenPage_Click">View on the website</Hyperlink>
        </TextBlock>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
          <ui:Button Name="SubmitButton" Content="Publish" Appearance="Primary" Margin="0,0,8,0" Click="Submit_Click" />
          <ui:Button Content="Cancel" Appearance="Secondary" IsCancel="True" Click="Cancel_Click" />
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</base:WpfUiWindow>
