<base:WpfUiWindow x:Class="Fedestrap.UI.Elements.Dialogs.FFlagSearchDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:base="clr-namespace:Fedestrap.UI.Elements.Base" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" Title="Find Fast Flags" Width="1000" Height="700" MinWidth="800" MinHeight="600" ExtendsContentIntoTitleBar="True" WindowStartupLocation="CenterOwner" xmlns:dialogs="clr-namespace:Fedestrap.UI.Elements.Dialogs" Background="{ui:ThemeResource ApplicationBackgroundBrush}">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <Style x:Key="SectionHeaderStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Margin" Value="0,10,0,5" />
      </Style>
      <Style x:Key="DescriptionStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Margin" Value="0,0,0,10" />
      </Style>
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <Grid>
    <Panel.Background>
      <LinearGradientBrush StartPoint="1,1" EndPoint="0,0">
        <GradientStop Offset="0.00" Color="{DynamicResource WindowBackgroundColorPrimary}" />
        <GradientStop Offset="0.80" Color="{DynamicResource WindowBackgroundColorSecondary}" />
        <GradientStop Offset="1.10" Color="{DynamicResource WindowBackgroundColorThird}" />
      </LinearGradientBrush>
    </Panel.Background>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <ui:TitleBar Title="Find Fast Flags" Grid.Row="0" Padding="8" CanMaximize="True" KeyboardNavigation.TabNavigation="None" ShowMaximize="True" ShowMinimize="True" />
    <StackPanel Grid.Row="1" Margin="20,10,20,0">
      <TextBlock Margin="0,0,0,10" FontSize="20" FontWeight="Bold" Text="Fast Flag Search &amp; Validation" />
      <TextBlock Style="{StaticResource DescriptionStyle}" Text="Search, validate, and discover Fast Flags from multiple sources" />
      <Border Margin="0,0,0,10" Padding="10" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="5">
        <StackPanel Orientation="Horizontal">
          <TextBlock Name="StatusText" VerticalAlignment="Center" Text="Ready" />
          <ProgressBar Name="LoadingProgress" Width="200" Height="10" Margin="10,0,0,0" IsIndeterminate="True" Visibility="Collapsed" />
        </StackPanel>
      </Border>
    </StackPanel>
    <TabControl Name="MainTabControl" Grid.Row="2" Margin="20,0,20,0">
      <TabItem>
        <TabItem.Header>
          <StackPanel Orientation="Horizontal">
            <ui:SymbolIcon Symbol="Search24" Margin="0,0,6,0" />
            <TextBlock Text="Live Search" />
          </StackPanel>
        </TabItem.Header>
        <Grid Margin="10">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="Live Flag Search" />
          <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Style="{StaticResource DescriptionStyle}" Text="Search by full flag name or partial keyword match" />
            <ui:TextBox Name="SearchTextBox" Height="35" PlaceholderText="Enter flag name or keyword..." TextChanged="SearchTextBox_TextChanged" />
            <StackPanel Margin="0,5,0,0" Orientation="Horizontal">
              <CheckBox Name="TrueFlagsOnlyCheckBox" Margin="0,0,15,0" Content="True flags only" Checked="TrueFlagsOnlyCheckBox_CheckedChanged" Unchecked="TrueFlagsOnlyCheckBox_CheckedChanged" />
              <CheckBox Name="FalseFlagsOnlyCheckBox" Margin="0,0,15,0" Content="False flags only" Checked="FalseFlagsOnlyCheckBox_CheckedChanged" Unchecked="FalseFlagsOnlyCheckBox_CheckedChanged" />
              <ui:Button Name="ExportSearchResultsButton" Content="Export Results" IsEnabled="False" Click="ExportSearchResultsButton_Click" />
            </StackPanel>
          </StackPanel>
          <DataGrid Name="SearchResultsDataGrid" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
            <DataGrid.Columns>
              <DataGridTextColumn Width="*" Header="Flag Name" Binding="{Binding Name}" />
              <DataGridTextColumn Width="150" Header="Value" Binding="{Binding Value}" />
            </DataGrid.Columns>
          </DataGrid>
          <TextBlock Name="SearchResultsCount" Grid.Row="3" Margin="0,5,0,0" HorizontalAlignment="Right" Text="0 results" />
        </Grid>
      </TabItem>
      <TabItem>
        <TabItem.Header>
          <StackPanel Orientation="Horizontal">
            <ui:SymbolIcon Symbol="Beaker24" Margin="0,0,6,0" />
            <TextBlock Text="Bulk Validation" />
          </StackPanel>
        </TabItem.Header>
        <Grid Margin="10">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="Bulk Flag Validation" />
          <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Style="{StaticResource DescriptionStyle}" Text="Type flags directly or upload a file for validation" />
            <StackPanel Margin="0,0,0,5" Orientation="Horizontal">
              <ui:Button Name="LoadFileButton" Margin="0,0,10,0" Content="Load File" Click="LoadFileButton_Click" />
              <ui:Button Name="ValidateButton" Margin="0,0,10,0" Content="Validate" Click="ValidateButton_Click" />
              <ui:Button Name="ClearValidationButton" Margin="0,0,10,0" Content="Clear" Click="ClearValidationButton_Click" />
            </StackPanel>
            <ui:TextBox Name="ValidationInputTextBox" Height="200" AcceptsReturn="True" AllowDrop="True" HorizontalScrollBarVisibility="Auto" PlaceholderText="Type or paste flags here in JSON format...&#xA;Example:&#xA;{&#xA;  &quot;FFlagDebugDisplayFPS&quot;: &quot;True&quot;,&#xA;  &quot;DFIntTaskSchedulerTargetFps&quot;: &quot;120&quot;&#xA;}" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto">
              <FrameworkElement.ContextMenu>
                <ContextMenu>
                  <MenuItem Header="Paste" InputGestureText="Ctrl+V" Click="PasteMenuItem_Click">
                    <MenuItem.Icon>
                      <ui:SymbolIcon Symbol="ClipboardPaste16" />
                    </MenuItem.Icon>
                  </MenuItem>
                  <Separator />
                  <MenuItem Header="Select All" InputGestureText="Ctrl+A" Click="SelectAllMenuItem_Click">
                    <MenuItem.Icon>
                      <ui:SymbolIcon Symbol="SelectAllOn20" />
                    </MenuItem.Icon>
                  </MenuItem>
                  <MenuItem Header="Clear" Click="ClearMenuItem_Click">
                    <MenuItem.Icon>
                      <ui:SymbolIcon Symbol="Delete16" />
                    </MenuItem.Icon>
                  </MenuItem>
                </ContextMenu>
              </FrameworkElement.ContextMenu>
            </ui:TextBox>
          </StackPanel>
          <TextBlock Grid.Row="2" Style="{StaticResource SectionHeaderStyle}" Text="Validation Results" />
          <DataGrid Name="ValidationResultsDataGrid" Grid.Row="3" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
            <DataGrid.Columns>
              <DataGridTextColumn Width="*" Header="Flag Name" Binding="{Binding Name}" />
              <DataGridTextColumn Width="150" Header="Input Value" Binding="{Binding InputValue}" />
              <DataGridTextColumn Width="100" Header="Status" Binding="{Binding Status}" />
              <DataGridTextColumn Width="150" Header="Valid Value" Binding="{Binding ValidValue}" />
              <DataGridTextColumn Width="200" Header="Notes" Binding="{Binding Notes}" />
            </DataGrid.Columns>
          </DataGrid>
          <StackPanel Grid.Row="4" Margin="0,10,0,0" Orientation="Horizontal">
            <ui:Button Name="ExportValidResultsButton" Margin="0,0,10,0" Content="Export Valid Flags" IsEnabled="False" Click="ExportValidResultsButton_Click" />
            <TextBlock Name="ValidationResultsCount" VerticalAlignment="Center" Text="0 results" />
          </StackPanel>
        </Grid>
      </TabItem>
      <TabItem>
        <TabItem.Header>
          <StackPanel Orientation="Horizontal">
            <ui:SymbolIcon Symbol="History24" Margin="0,0,6,0" />
            <TextBlock Text="Recent Flags (24h)" />
          </StackPanel>
        </TabItem.Header>
        <Grid Margin="10">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="Recent Flags (Last 24 Hours)" />
          <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Style="{StaticResource DescriptionStyle}" Text="Discover flags that were added or modified in the last 24 hours" />
            <StackPanel Orientation="Horizontal">
              <ui:Button Name="FetchRecentButton" Margin="0,0,10,0" Content="Fetch Recent Flags" Click="FetchRecentButton_Click" />
              <ui:Button Name="DownloadAllRecentButton" Margin="0,0,10,0" Content="Download All" IsEnabled="False" Click="DownloadAllRecentButton_Click" />
              <ui:Button Name="DownloadTrueRecentButton" Margin="0,0,10,0" Content="Download True Only" IsEnabled="False" Click="DownloadTrueRecentButton_Click" />
              <ui:Button Name="DownloadFalseRecentButton" Content="Download False Only" IsEnabled="False" Click="DownloadFalseRecentButton_Click" />
            </StackPanel>
          </StackPanel>
          <DataGrid Name="RecentFlagsDataGrid" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
            <DataGrid.Columns>
              <DataGridTextColumn Width="*" Header="Flag Name" Binding="{Binding Name}" />
              <DataGridTextColumn Width="150" Header="Value" Binding="{Binding Value}" />
              <DataGridTextColumn Width="150" Header="Date Added" Binding="{Binding DateAdded}" />
            </DataGrid.Columns>
          </DataGrid>
          <TextBlock Name="RecentFlagsCount" Grid.Row="3" Margin="0,5,0,0" HorizontalAlignment="Right" Text="0 recent flags" />
        </Grid>
      </TabItem>
    </TabControl>
    <StackPanel Grid.Row="3" Margin="20,10,20,20" HorizontalAlignment="Right" Orientation="Horizontal">
      <ui:Button Name="CloseButton" Width="80" Content="Close" Click="CloseButton_Click" />
    </StackPanel>
  </Grid>
</base:WpfUiWindow>