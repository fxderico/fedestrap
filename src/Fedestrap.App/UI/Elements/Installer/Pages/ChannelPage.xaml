<ui:UiPage x:Class="Fedestrap.UI.Elements.Installer.Pages.ChannelPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:Fedestrap.UI.Elements.Controls" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:Fedestrap.UI.Elements.Installer.Pages" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:resources="clr-namespace:Fedestrap.Resources" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" Title="ChannelPage" Scrollable="True" Loaded="UiPage_Loaded" Unloaded="UiPage_Unloaded">
  <StackPanel Margin="0,0,14,14">
    <TextBlock FontSize="20" FontWeight="SemiBold" Text="Download Server" TextWrapping="Wrap" />
    <TextBlock Margin="0,4,0,0" FontSize="14" LineHeight="20" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Text="Choose the server used when Roblox is installed or updated." />
        <controls:OptionControl Margin="0,0,0,4" Header="Download server" Description="Used when Roblox is installed or updated">
      <ComboBox MinWidth="300" Height="35" Padding="10,5,10,5" VerticalContentAlignment="Center" DisplayMemberPath="Display" ItemsSource="{Binding Mirrors}" SelectedItem="{Binding SelectedMirror, Mode=TwoWay}" />
    </controls:OptionControl>

    <TextBlock Margin="0,24,0,0" FontSize="20" FontWeight="SemiBold" Text="Channel" TextWrapping="Wrap" />
    <TextBlock Margin="0,4,0,0" FontSize="14" LineHeight="20" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Text="The deployment channel Roblox is installed from. Production is the default and is what almost everyone wants. Other channels such as LIVE or zlive are Roblox test builds and may be unavailable, which shows up as a 403 or 404 while downloading" />
    <controls:OptionControl Margin="0,0,0,4" Header="Preferred channel" Description="Leave this as Production unless you know the channel you are typing exists">
      <ui:TextBox MinWidth="300" Height="35" VerticalContentAlignment="Center" PlaceholderText="Production" Text="{Binding Channel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
    </controls:OptionControl>
  </StackPanel>
</ui:UiPage>
