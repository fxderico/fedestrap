<ui:UiPage x:Class="Fedestrap.UI.Elements.Installer.Pages.InstallPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:Fedestrap.UI.Elements.Controls" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:Fedestrap.UI.Elements.Installer.Pages" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:resources="clr-namespace:Fedestrap.Resources" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" Title="InstallPage" Scrollable="True" Loaded="UiPage_Loaded">
  <StackPanel Margin="0,0,14,14">
    <TextBlock FontSize="20" FontWeight="SemiBold" Text="{x:Static resources:Strings.Installer_Install_Location_Title}" TextWrapping="Wrap" />
    <TextBlock Margin="0,4,0,0" FontSize="14" LineHeight="20" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{x:Static resources:Strings.Installer_Install_Location_Text}" TextWrapping="Wrap" />
    <ui:Card Margin="0,12,0,0" Padding="12">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <ui:TextBox Grid.Column="0" Height="35" VerticalContentAlignment="Center" Text="{Binding InstallLocation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <ui:Button Grid.Column="1" Height="35" MinWidth="104" Margin="8,0,0,0" HorizontalContentAlignment="Center" Content="{x:Static resources:Strings.Common_Browse}" Icon="Folder24" Command="{Binding BrowseInstallLocationCommand}" />
        <ui:Button Grid.Column="2" Height="35" MinWidth="104" Margin="8,0,0,0" HorizontalContentAlignment="Center" Content="{x:Static resources:Strings.Common_Reset}" Icon="ArrowCounterclockwise24" Command="{Binding ResetInstallLocationCommand}" />
      </Grid>
    </ui:Card>
    <TextBlock Margin="0,10,0,0" FontSize="14" LineHeight="20" TextWrapping="Wrap" Text="{x:Static resources:Strings.Installer_Install_Location_DataFound}" Visibility="{Binding DataFoundMessageVisibility, Mode=OneWay}" />
    <TextBlock Margin="0,10,0,0" FontSize="14" LineHeight="20" Foreground="{DynamicResource SystemFillColorCriticalBrush}" TextWrapping="Wrap" Text="{Binding ErrorMessage, Mode=OneWay}">
      <FrameworkElement.Style>
        <Style TargetType="{x:Type TextBlock}">
          <Style.Triggers>
            <DataTrigger Value="" Binding="{Binding Path=Text, RelativeSource={RelativeSource Mode=Self}}">
              <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </FrameworkElement.Style>
    </TextBlock>
    <TextBlock Margin="0,28,0,0" FontSize="20" FontWeight="SemiBold" Text="{x:Static resources:Strings.Common_Shortcuts}" TextWrapping="Wrap" />
    <TextBlock Margin="0,4,0,0" FontSize="14" LineHeight="20" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{x:Static resources:Strings.Menu_Shortcuts_Description}" TextWrapping="Wrap" />
    <controls:OptionControl Margin="0,0,0,4" Header="{x:Static resources:Strings.Menu_Shortcuts_ExtractIcons_Title}" HelpLink="https://github.com/BloxstrapLabs/Bloxstrap/wiki/Using-Roblox-icons-for-your-shortcuts" Description="{Binding Source={x:Static resources:Strings.Menu_Shortcuts_ExtractIcons_Description}, Converter={StaticResource StringFormatConverter}, ConverterParameter=https://github.com/BloxstrapLabs/Bloxstrap/wiki/Using-Roblox-icons-for-your-shortcuts}">
      <ui:ToggleSwitch IsChecked="{Binding ExtractRobloxIcons, Mode=TwoWay}" />
    </controls:OptionControl>
    <TextBlock Margin="0,24,0,0" FontSize="20" FontWeight="SemiBold" Text="{x:Static resources:Strings.Menu_Shortcuts_General_Title}" TextWrapping="Wrap" />
    <TextBlock Margin="0,4,0,0" FontSize="14" LineHeight="20" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{x:Static resources:Strings.Menu_Shortcuts_General_Description}" TextWrapping="Wrap" />
    <Grid Margin="0,0,0,4">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <controls:OptionControl Grid.Column="0" Margin="0,0,4,0" Header="{x:Static resources:Strings.Common_Shortcuts_Desktop}">
        <ui:ToggleSwitch IsChecked="{Binding CreateDesktopShortcuts, Mode=TwoWay}" />
      </controls:OptionControl>
      <controls:OptionControl Grid.Column="1" Margin="4,0,0,0" Header="{x:Static resources:Strings.Common_Shortcuts_StartMenu}">
        <ui:ToggleSwitch IsChecked="{Binding CreateStartMenuShortcuts, Mode=TwoWay}" />
      </controls:OptionControl>
    </Grid>
    <TextBlock Margin="0,24,0,0" FontSize="20" FontWeight="SemiBold" Text="{x:Static resources:Strings.Menu_Shortcuts_Function_Title}" TextWrapping="Wrap" />
    <TextBlock Margin="0,4,0,0" FontSize="14" LineHeight="20" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{x:Static resources:Strings.Menu_Shortcuts_Function_Description}" TextWrapping="Wrap" />
    <Grid Margin="0,0,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <controls:OptionControl Grid.Column="0" Margin="0,0,4,0" Header="{x:Static resources:Strings.LaunchMenu_LaunchRoblox}">
        <ui:ToggleSwitch IsChecked="{Binding CreatePlayerShortcut, Mode=TwoWay}" />
      </controls:OptionControl>
      <controls:OptionControl Grid.Column="1" Margin="4,0,4,0" Header="Roblox Studio">
        <ui:ToggleSwitch IsChecked="{Binding CreateStudioShortcut, Mode=TwoWay}" />
      </controls:OptionControl>
      <controls:OptionControl Grid.Column="2" Margin="4,0,0,0" Header="{x:Static resources:Strings.Menu_Title}">
        <ui:ToggleSwitch IsChecked="{Binding CreateSettingsShortcut, Mode=TwoWay}" />
      </controls:OptionControl>
    </Grid>
  </StackPanel>
</ui:UiPage>