<ui:UiPage x:Class="Fedestrap.UI.Elements.Settings.Pages.FriendsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:util="clr-namespace:Fedestrap.Utility"
    xmlns:controls="clr-namespace:Fedestrap.UI.Elements.Controls"
    Title="Friends" Scrollable="True" Loaded="Page_Loaded">
  <ui:UiPage.Resources>
    <Style x:Key="FriendCard" TargetType="ui:Card" BasedOn="{StaticResource {x:Type ui:Card}}">
      <Setter Property="Margin" Value="0,0,12,12" />
      <Setter Property="Padding" Value="14" />
      <Setter Property="MinHeight" Value="150" />
      <Setter Property="VerticalAlignment" Value="Stretch" />
      <Setter Property="HorizontalAlignment" Value="Stretch" />
    </Style>
  </ui:UiPage.Resources>

  <Grid SizeChanged="FriendsPage_SizeChanged">
    <StackPanel Margin="0,0,14,14">

      <Grid Margin="0,0,0,4">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
          <ui:SymbolIcon Symbol="People24" FontSize="22" Margin="0,0,8,0" VerticalAlignment="Center" />
          <TextBlock Text="Friends" FontSize="20" FontWeight="Medium" VerticalAlignment="Center" />
          <Border Margin="10,0,0,0" Padding="8,2,8,2" VerticalAlignment="Center" CornerRadius="10" Background="{DynamicResource ControlFillColorSecondaryBrush}" Visibility="{Binding CountBadgeVisibility}">
            <TextBlock FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding FriendCountText}" />
          </Border>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
          <ui:Button Content="Back" Icon="ArrowLeft24" Appearance="Secondary" Click="Back_Click" />
          <ui:Button Margin="8,0,0,0" Content="Refresh" Icon="ArrowClockwise24" Appearance="Secondary" IsEnabled="{Binding CanRefresh}" Click="Refresh_Click" />
        </StackPanel>
      </Grid>

      <TextBlock Margin="0,0,0,12" FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Everyone you are friends with on Fedestrap." />

      <ui:TextBox Width="320" Height="34" Margin="0,0,0,14" HorizontalAlignment="Left" VerticalContentAlignment="Center"
                  Icon="Search24" ClearButtonEnabled="True" PlaceholderText="Search friends"
                  Visibility="{Binding SearchVisibility}"
                  Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

      <Border Padding="30" CornerRadius="7" BorderThickness="1" HorizontalAlignment="Stretch"
              BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
              Background="{DynamicResource ControlFillColorDefaultBrush}"
              Visibility="{Binding StatePanelVisibility}">
        <StackPanel HorizontalAlignment="Center">
          <ui:ProgressRing IsIndeterminate="True" Width="32" Height="32" HorizontalAlignment="Center" Visibility="{Binding SpinnerVisibility}" />
          <ui:SymbolIcon Symbol="{Binding StateIcon}" FontSize="34" HorizontalAlignment="Center" Opacity="0.55" Visibility="{Binding StateIconVisibility}" />
          <TextBlock Margin="0,12,0,0" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center" TextAlignment="Center" Text="{Binding StateTitle}" />
          <TextBlock Margin="0,4,0,0" FontSize="12" Opacity="0.7" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" MaxWidth="460" Text="{Binding StateSubtitle}" />
        </StackPanel>
      </Border>

      <ItemsControl x:Name="FriendsList" ItemsSource="{Binding FilteredFriends}" Visibility="{Binding ListVisibility}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <UniformGrid HorizontalAlignment="Stretch" Columns="{Binding DataContext.GridColumns, RelativeSource={RelativeSource AncestorType=ui:UiPage}}" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <ui:Card Style="{StaticResource FriendCard}">
              <Grid>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="*" />
                  <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                  </Grid.ColumnDefinitions>
                  <Grid Grid.Column="0" Width="96" Height="96" Margin="0,-19,0,-19" VerticalAlignment="Top" ClipToBounds="False">
                    <Border Width="58" Height="58" CornerRadius="29" Padding="3" HorizontalAlignment="Center" VerticalAlignment="Center" Background="{Binding RingBrush}">
                      <Border CornerRadius="26" ClipToBounds="True" Background="{DynamicResource ControlFillColorSecondaryBrush}">
                        <Image util:DynamicRenderSystem.LazyImageSource="{Binding Avatar}" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality" />
                      </Border>
                    </Border>
                    <Image Source="{Binding BorderImageSource}" Width="{Binding BorderImageWidth}" Height="{Binding BorderImageHeight}" Margin="{Binding BorderImageMargin}" Panel.ZIndex="{Binding BorderImageZ}" HorizontalAlignment="Left" VerticalAlignment="Top" IsHitTestVisible="False" RenderOptions.BitmapScalingMode="HighQuality" />
                  </Grid>
                  <StackPanel Grid.Column="1" Margin="12,4,0,0" VerticalAlignment="Top">
                    <TextBlock FontSize="15" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Text="{Binding DisplayName}" ToolTip="{Binding DisplayName}" />
                    <TextBlock Margin="0,1,0,0" FontSize="12" TextTrimming="CharacterEllipsis" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="{Binding UsernameDisplay}" />
                  </StackPanel>
                </Grid>

                <ItemsControl Grid.Row="1" Margin="0,10,0,10" VerticalAlignment="Top" ItemsSource="{Binding FriendBadges}">
                  <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                      <WrapPanel />
                    </ItemsPanelTemplate>
                  </ItemsControl.ItemsPanel>
                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <Image Width="18" Height="18" Margin="0,0,4,4" Source="{Binding Image}" Stretch="UniformToFill" ToolTip="{Binding Name}" RenderOptions.BitmapScalingMode="HighQuality" />
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>

                <ui:Button Grid.Row="2" HorizontalAlignment="Left" Appearance="Secondary" Content="Remove friend" Icon="PersonDelete24" Click="Unfriend_Click" Tag="{Binding Id}" />
              </Grid>
            </ui:Card>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>

    </StackPanel>
  </Grid>
</ui:UiPage>
