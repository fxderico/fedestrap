<ui:UiPage x:Class="Fedestrap.UI.Elements.Settings.Pages.ReleasesPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" xmlns:conv="clr-namespace:Fedestrap.UI.Converters" x:Name="ReleasesView" Title="Releases" Scrollable="True" xmlns:pages="clr-namespace:Fedestrap.UI.Elements.Settings.Pages">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <conv:BooleanToVisibilityConverter x:Key="BoolToVis" />
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <ui:TextBox Name="SearchBox" Height="32" Margin="0,0,0,12" HorizontalContentAlignment="Left" VerticalContentAlignment="Center" BorderThickness="0" FontSize="14" Icon="Search48" PlaceholderText="Search Releases..." FocusVisualStyle="{x:Null}" TextChanged="SearchBox_TextChanged" />
    <ui:DynamicScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
      <ItemsControl ItemsSource="{Binding Releases}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Margin="0,0,0,12" Padding="10" CornerRadius="8" BorderThickness="1" BorderBrush="#803A3A3A">
              <StackPanel>
                <TextBlock FontSize="16" FontWeight="Bold" Text="{Binding Name}" />
                <StackPanel Margin="0,4,0,4" Orientation="Horizontal">
                  <TextBlock Margin="0,0,8,0" FontWeight="SemiBold" Foreground="#FF1E90FF" Text="{Binding TagName}" />
                  <TextBlock Margin="0,0,8,0" Foreground="#FF808080" Text="{Binding PublishedAt, StringFormat=Published: {0:yyyy-MM-dd HH:mm}}" />
                  <TextBlock Margin="0,0,8,0" Foreground="#FFFF4500" Text="Pre-release" Visibility="{Binding Prerelease, Converter={StaticResource BoolToVis}}" />
                  <TextBlock Margin="0,0,8,0" Foreground="#FFFF6347" Text="Draft" Visibility="{Binding Draft, Converter={StaticResource BoolToVis}}" />
                </StackPanel>
                <TextBlock Margin="0,0,0,4">
                  <Hyperlink NavigateUri="{Binding HtmlUrl}" RequestNavigate="Hyperlink_RequestNavigate">
                    <Run Text="View on GitHub" />
                  </Hyperlink>
                </TextBlock>
                <TextBox Margin="0,4,0,4" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" BorderThickness="0" Background="#00FFFFFF" Foreground="#FFFFFFFF" VerticalScrollBarVisibility="Auto" Height="120" Text="{Binding Body}" />
                <StackPanel Margin="0,4,0,4" Orientation="Horizontal">
                  <TextBlock Margin="0,0,12,0" Foreground="#FF808080" Text="{Binding Assets.Length, StringFormat=Assets: {0}}" />
                  <TextBlock Margin="0,0,12,0" Foreground="#FF808080" Text="{Binding TotalDownloads, StringFormat=Total downloads: {0}}" />
                </StackPanel>
                <ItemsControl Margin="0,4,0,0" ItemsSource="{Binding Assets}">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <Border Margin="0,2,0,2" Padding="6" CornerRadius="4" Background="#202020">
                        <StackPanel Orientation="Horizontal">
                          <TextBlock Margin="0,0,8,0">
                            <Hyperlink NavigateUri="{Binding BrowserDownloadUrl}" RequestNavigate="Hyperlink_RequestNavigate">
                              <Run Text="{Binding Name}" />
                            </Hyperlink>
                          </TextBlock>
                          <TextBlock Margin="0,0,8,0" Foreground="#FF808080" Text="{Binding SizeMb, StringFormat=({0:F2} MB)}" />
                          <TextBlock Margin="0,0,8,0" Foreground="#FF808080" Text="{Binding DownloadCount, StringFormat=Downloads: {0}}" />
                          <TextBlock Foreground="#FF808080" Text="{Binding ContentType}" />
                        </StackPanel>
                      </Border>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </StackPanel>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ui:DynamicScrollViewer>
  </Grid>
</ui:UiPage>