<ui:UiPage x:Class="Fedestrap.UI.Elements.Settings.Pages.ModsPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:Fedestrap.UI.Elements.Controls" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:models="clr-namespace:Fedestrap.UI.ViewModels" xmlns:pages="clr-namespace:Fedestrap.UI.Elements.Settings.Pages" xmlns:resources="clr-namespace:Fedestrap.Resources" xmlns:conv="clr-namespace:Fedestrap.Converters" xmlns:converters="clr-namespace:Fedestrap.UI.Converters" xmlns:settings="clr-namespace:Fedestrap.UI.ViewModels.Settings" xmlns:platform="clr-namespace:Fedestrap.UI" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" xmlns:gif="clr-namespace:WpfAnimatedGif;assembly=WpfAnimatedGif" Title="ModsPage" Scrollable="True">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <conv:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter" />
      <conv:EnumNotEqualsVisibilityConverter x:Key="EnumNotEqualsVisibilityConverter" />
      <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <StackPanel Margin="0,0,14,14">
    <StackPanel Visibility="{Binding MainContentVisibility}">
      <TextBlock FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{x:Static resources:Strings.Menu_Mods_Description}" TextWrapping="Wrap" />
      <Grid Margin="0,16,0,0">
        <Grid.RowDefinitions>
          <RowDefinition Height="*" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ui:CardAction Name="OpenModFolderCardAction" Grid.Row="0" Grid.Column="0" Margin="0,0,4,0" Icon="Folder24" Command="{Binding OpenModsFolderCommand}">
          <StackPanel>
            <TextBlock FontSize="14" Text="{x:Static resources:Strings.Menu_Mods_OpenModsFolder_Title}" TextWrapping="Wrap" FontWeight="Medium" />
            <TextBlock Margin="0,2,0,0" FontSize="12" Text="{x:Static resources:Strings.Menu_Mods_OpenModsFolder_Description}" Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" />
          </StackPanel>
        </ui:CardAction>
        <ui:CardAction Grid.Row="1" Grid.Column="0" Margin="0,7,4,0" Icon="DocumentSearch24" Command="{Binding ToggleModExplorerCommand}">
          <StackPanel>
            <TextBlock FontSize="14" Text="Mod Explorer" FontWeight="Medium" />
            <TextBlock Margin="0,2,0,0" FontSize="12" Text="View and manage files in your Roblox installation." Padding="0,0,16,0" Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" />
          </StackPanel>
        </ui:CardAction>
        <ui:CardAction Grid.Row="1" Grid.Column="1" Margin="4,7,0,0" Icon="BookQuestionMark24" Command="models:GlobalViewModel.OpenWebpageCommand" CommandParameter="https://github.com/Bloxstraplabs/Bloxstrap/wiki/Adding-custom-mods">
          <StackPanel>
            <TextBlock FontSize="14" Text="{x:Static resources:Strings.Common_Help}" FontWeight="Medium" />
            <TextBlock Margin="0,2,0,0" FontSize="12" Text="{x:Static resources:Strings.Menu_Mods_Help_Description}" Padding="0,0,16,0" Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" />
          </StackPanel>
        </ui:CardAction>
      </Grid>
      <ui:CardAction Margin="0,7,0,0" VerticalAlignment="Top" Icon="WindowWrench24" Command="{Binding OpenCompatSettingsCommand}">
        <StackPanel>
          <TextBlock FontSize="14" Text="{x:Static resources:Strings.Menu_Mods_Misc_CompatibilitySettings_Title}" TextWrapping="Wrap" FontWeight="Medium" />
          <controls:MarkdownTextBlock Margin="0,2,0,0" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" MarkdownText="{Binding Source={x:Static resources:Strings.Menu_Mods_Misc_CompatibilitySettings_Description}, Converter={StaticResource StringFormatConverter}, ConverterParameter=https://devblogs.microsoft.com/directx/demystifying-full-screen-optimizations/}" />
        </StackPanel>
      </ui:CardAction>
      <TabControl Name="ModsTabs" Margin="0,16,0,0">
        <TabItem FontWeight="Medium">
          <TabItem.Header>
            <StackPanel Orientation="Horizontal">
              <ui:SymbolIcon Symbol="Color24" Margin="0,0,6,0" />
              <TextBlock Text="Preset Mod" />
            </StackPanel>
          </TabItem.Header>
          <StackPanel Margin="12,12,12,12" Height="Auto">
            <TextBlock FontSize="20" FontWeight="Bold" Margin="0,0,0,8" Text="{x:Static resources:Strings.Common_Presets}" />
            <controls:OptionControl Description="Choose whether your mods are applied to the Roblox player, Roblox Studio, or both." Header="Apply mods to">
              <ComboBox Width="200" Margin="5,0,0,0" Padding="10,5,10,5" ItemsSource="{Binding ModApplyTargets, Mode=OneTime}" SelectedItem="{Binding ModApplyTarget, Mode=TwoWay}">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <TextBlock Text="{Binding Path=., Converter={StaticResource EnumNameConverter}}" />
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ComboBox>
            </controls:OptionControl>
            <controls:OptionControl Description="{x:Static resources:Strings.Menu_Mods_Presets_OldAvatarEditor_Description}" Header="{x:Static resources:Strings.Menu_Mods_Presets_OldAvatarEditor_Title}">
              <ui:ToggleSwitch IsChecked="{Binding OldAvatarBackgroundTask.NewState, Mode=TwoWay}" />
            </controls:OptionControl>
            <controls:OptionControl Description="{x:Static resources:Strings.Menu_Mods_Presets_OldCharacterSounds_Description}" Header="{x:Static resources:Strings.Menu_Mods_Presets_OldCharacterSounds_Title}">
              <ui:ToggleSwitch IsChecked="{Binding OldCharacterSoundsTask.NewState, Mode=TwoWay}" />
            </controls:OptionControl>
            <controls:OptionControl Description="{x:Static resources:Strings.Menu_Mods_Presets_MouseCursor_Description}" Header="{x:Static resources:Strings.Menu_Mods_Presets_MouseCursor_Title}">
              <ComboBox Width="200" Margin="5,0,0,0" Padding="10,5,10,5" ItemsSource="{Binding CursorTypeTask.Selections, Mode=OneTime}" Text="{Binding CursorTypeTask.NewState, Mode=TwoWay}">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <TextBlock Text="{Binding Path=., Converter={StaticResource EnumNameConverter}}" />
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ComboBox>
            </controls:OptionControl>
            <controls:OptionControl Description="{x:Static resources:Strings.Menu_Mods_Presets_EmojiType_Description}" Header="{x:Static resources:Strings.Menu_Mods_Presets_EmojiType_Title}">
              <ComboBox Width="220" Margin="5,0,0,0" Padding="10,5,10,5" ItemsSource="{Binding EmojiFontTask.Selections, Mode=OneTime}" Text="{Binding EmojiFontTask.NewState, Mode=TwoWay}">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <TextBlock Text="{Binding Path=., Converter={StaticResource EnumNameConverter}}" />
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ComboBox>
            </controls:OptionControl>
            <TextBlock Margin="0,16,0,0" FontSize="20" FontWeight="Bold" Text="{x:Static resources:Strings.Common_Miscellaneous}" />
            <controls:OptionControl Header="Skybox Manager" Description="Choose a skybox once. Fedestrap downloads and applies it safely when Roblox starts.">
              <StackPanel Width="360">
                <ui:CardExpander Margin="0,0,0,8" IsExpanded="False">
                  <HeaderedContentControl.Header>
                    <StackPanel>
                      <TextBlock FontSize="14" Text="Custom skybox" />
                      <TextBlock Margin="0,2,0,0" FontSize="12" Text="Use one image or choose every cube face." Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                    </StackPanel>
                  </HeaderedContentControl.Header>
                  <StackPanel>
                    <ui:Button HorizontalAlignment="Stretch" Content="Use one image for every side" Icon="ImageAdd24" Command="{Binding ChooseSingleSkyboxImageCommand}" IsEnabled="{Binding CustomSkyboxControlsEnabled}" />
                    <Grid Margin="0,8,0,0">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="52" />
                        <ColumnDefinition Width="*" />
                      </Grid.ColumnDefinitions>
                      <TextBlock VerticalAlignment="Center" Text="Back" />
                      <ui:Button Grid.Column="1" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Content="{Binding CustomSkyboxBack}" ToolTip="{Binding CustomSkyboxBack}" Command="{Binding ChooseSkyboxFaceCommand}" CommandParameter="Back" IsEnabled="{Binding CustomSkyboxControlsEnabled}" />
                    </Grid>
                    <Grid Margin="0,6,0,0">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="52" />
                        <ColumnDefinition Width="*" />
                      </Grid.ColumnDefinitions>
                      <TextBlock VerticalAlignment="Center" Text="Down" />
                      <ui:Button Grid.Column="1" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Content="{Binding CustomSkyboxDown}" ToolTip="{Binding CustomSkyboxDown}" Command="{Binding ChooseSkyboxFaceCommand}" CommandParameter="Down" IsEnabled="{Binding CustomSkyboxControlsEnabled}" />
                    </Grid>
                    <Grid Margin="0,6,0,0">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="52" />
                        <ColumnDefinition Width="*" />
                      </Grid.ColumnDefinitions>
                      <TextBlock VerticalAlignment="Center" Text="Front" />
                      <ui:Button Grid.Column="1" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Content="{Binding CustomSkyboxFront}" ToolTip="{Binding CustomSkyboxFront}" Command="{Binding ChooseSkyboxFaceCommand}" CommandParameter="Front" IsEnabled="{Binding CustomSkyboxControlsEnabled}" />
                    </Grid>
                    <Grid Margin="0,6,0,0">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="52" />
                        <ColumnDefinition Width="*" />
                      </Grid.ColumnDefinitions>
                      <TextBlock VerticalAlignment="Center" Text="Left" />
                      <ui:Button Grid.Column="1" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Content="{Binding CustomSkyboxLeft}" ToolTip="{Binding CustomSkyboxLeft}" Command="{Binding ChooseSkyboxFaceCommand}" CommandParameter="Left" IsEnabled="{Binding CustomSkyboxControlsEnabled}" />
                    </Grid>
                    <Grid Margin="0,6,0,0">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="52" />
                        <ColumnDefinition Width="*" />
                      </Grid.ColumnDefinitions>
                      <TextBlock VerticalAlignment="Center" Text="Right" />
                      <ui:Button Grid.Column="1" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Content="{Binding CustomSkyboxRight}" ToolTip="{Binding CustomSkyboxRight}" Command="{Binding ChooseSkyboxFaceCommand}" CommandParameter="Right" IsEnabled="{Binding CustomSkyboxControlsEnabled}" />
                    </Grid>
                    <Grid Margin="0,6,0,0">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="52" />
                        <ColumnDefinition Width="*" />
                      </Grid.ColumnDefinitions>
                      <TextBlock VerticalAlignment="Center" Text="Up" />
                      <ui:Button Grid.Column="1" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Content="{Binding CustomSkyboxUp}" ToolTip="{Binding CustomSkyboxUp}" Command="{Binding ChooseSkyboxFaceCommand}" CommandParameter="Up" IsEnabled="{Binding CustomSkyboxControlsEnabled}" />
                    </Grid>
                    <WrapPanel Margin="0,10,0,0" HorizontalAlignment="Right">
                      <ui:ProgressRing Width="20" Height="20" Margin="0,0,8,0" IsIndeterminate="True" Visibility="{Binding CustomSkyboxBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
                      <ui:Button Margin="0,0,8,0" Appearance="Danger" Content="Remove custom" Icon="Delete16" Command="{Binding RemoveCustomSkyboxCommand}" IsEnabled="{Binding CustomSkyboxControlsEnabled}" Visibility="{Binding HasCustomSkybox, Converter={StaticResource BooleanToVisibilityConverter}}" />
                      <ui:Button Appearance="Primary" Content="Save and use" Command="{Binding ApplyCustomSkyboxCommand}" IsEnabled="{Binding CustomSkyboxCanApply}" />
                    </WrapPanel>
                  </StackPanel>
                </ui:CardExpander>
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>
                  <ComboBox Grid.Column="0" MinWidth="240" Padding="10,5,10,5" DisplayMemberPath="Name" ItemsSource="{Binding AvailableSkyboxPacks}" SelectedItem="{Binding SelectedSkyboxPack, Mode=TwoWay}" IsEnabled="{Binding SkyboxManagerReady}" />
                  <ui:Button Grid.Column="1" Margin="8,0,0,0" Icon="ArrowClockwise24" ToolTip="Refresh skyboxes" Command="{Binding RefreshSkyboxesCommand}" IsEnabled="{Binding SkyboxManagerReady}" />
                </Grid>
                <Grid Margin="0,8,0,0">
                  <ui:ToggleSwitch HorizontalAlignment="Right" IsChecked="{Binding SkyboxEnabled, Mode=TwoWay}" IsEnabled="{Binding SkyboxManagerReady}" />
                </Grid>
              </StackPanel>
            </controls:OptionControl>
            <controls:OptionControl Header="Font Manager" Description="Use a local font or use google fonts">
              <StackPanel Width="360">
                <Border Margin="0,0,0,8" Padding="9" MinHeight="54" CornerRadius="6" BorderThickness="1" BorderBrush="{DynamicResource ControlElevationBorderBrush}" Background="{DynamicResource ControlFillColorDefaultBrush}" Visibility="{Binding FontPreviewVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                  <TextBlock VerticalAlignment="Center" FontFamily="{Binding FontPreviewFontFamily}" FontSize="18" Text="Aa Bb Cc 123   The quick brown fox." TextWrapping="Wrap" />
                </Border>
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>
                  <ComboBox Grid.Column="0" MinWidth="240" MaxDropDownHeight="360" Padding="10,5,10,5" IsEditable="True" IsTextSearchEnabled="True" StaysOpenOnEdit="True" TextSearch.TextPath="Family" DisplayMemberPath="DisplayName" ItemsSource="{Binding AvailableGoogleFonts}" SelectedItem="{Binding SelectedGoogleFont, Mode=TwoWay}" IsEnabled="{Binding FontManagerReady}" ScrollViewer.CanContentScroll="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" />
                  <ui:Button Grid.Column="1" Margin="8,0,0,0" Icon="ArrowClockwise24" ToolTip="Refresh fonts" Command="{Binding RefreshGoogleFontsCommand}" IsEnabled="{Binding FontManagerReady}" />
                </Grid>
                <WrapPanel Margin="0,8,0,0" HorizontalAlignment="Right">
                  <ui:Button Margin="0,0,8,0" Content="Choose local font" Icon="DocumentAdd16" Command="{Binding ChooseLocalFontCommand}" IsEnabled="{Binding FontManagerReady}" Visibility="{Binding ChooseCustomFontVisibility}" />
                  <ui:Button Margin="0,0,8,0" Content="Remove font" Appearance="Danger" Icon="Delete16" Command="{Binding RemoveCustomFontCommand}" IsEnabled="{Binding FontManagerReady}" Visibility="{Binding DeleteCustomFontVisibility}" />
                  <ui:Button Content="Use font" Appearance="Primary" Command="{Binding ApplyGoogleFontCommand}" IsEnabled="{Binding FontManagerCanApply}" />
                </WrapPanel>
                <Grid Margin="0,9,0,0" Visibility="{Binding HasCustomFont, Converter={StaticResource BooleanToVisibilityConverter}}">
                  <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <TextBlock Margin="0,0,8,0" VerticalAlignment="Center" Text="Size" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <ComboBox MinWidth="100" ItemsSource="{Binding CustomFontScaleOptions, Mode=OneTime}" SelectedItem="{Binding CustomFontScale, Mode=TwoWay}" />
                  </StackPanel>
                </Grid>
              </StackPanel>
            </controls:OptionControl>
            <controls:OptionControl Header="Custom Death Sound" Description="Replaces the death sound with your own audio.">
              <StackPanel>
                <ui:Button Content="Choose Sound..." Icon="DocumentAdd16" Command="{Binding AddCustomDeathSoundCommand}" Visibility="{Binding ChooseCustomDeathSoundVisibility, Mode=OneWay}" />
                <ui:Button Appearance="Danger" Content="Remove Custom Death Sound" Icon="Delete16" Command="{Binding RemoveCustomDeathSoundCommand}" Visibility="{Binding DeleteCustomDeathSoundVisibility, Mode=OneWay}" />
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0" HorizontalAlignment="Right" Visibility="{Binding CustomDeathSoundVolumeVisibility, Mode=OneWay}">
                  <TextBlock Text="Volume" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                  <ComboBox MinWidth="110" ItemsSource="{Binding CustomDeathSoundVolumeOptions, Mode=OneTime}" SelectedItem="{Binding CustomDeathSoundVolume, Mode=TwoWay}" />
                </StackPanel>
              </StackPanel>
            </controls:OptionControl>
            <ui:CardExpander Grid.Row="1" Margin="0,8,0,0" IsExpanded="False">
              <HeaderedContentControl.Header>
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>
                  <StackPanel Grid.Column="0">
                    <TextBlock FontSize="14" Text="Custom Cursor Features" />
                    <TextBlock Margin="0,2,0,0" FontSize="12" Text="All of the Custom Cursor features you need." Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                  </StackPanel>
                </Grid>
              </HeaderedContentControl.Header>
              <StackPanel>
                <controls:OptionControl Description="Use your own Cursor in Roblox" Header="Custom Cursor">
                  <StackPanel>
                    <ui:Button Content="Choose" Icon="DocumentAdd16" Command="{Binding AddCustomCursorModCommand}" Visibility="{Binding ChooseCustomCursorVisibility, Mode=OneWay}" />
                    <ui:Button Appearance="Danger" Content="Remove" Icon="Delete16" Command="{Binding RemoveCustomCursorModCommand}" Visibility="{Binding DeleteCustomCursorVisibility, Mode=OneWay}" />
                  </StackPanel>
                </controls:OptionControl>
                <controls:OptionControl Description="Use your own Shiftlock in Roblox." Header="Custom Shiftlock">
                  <StackPanel>
                    <ui:Button Content="Choose Custom Shiftlock" Icon="DocumentAdd16" Command="{Binding AddCustomShiftlockModCommand}" Visibility="{Binding ChooseCustomShiftlockVisibility, Mode=OneWay}" />
                    <ui:Button Appearance="Danger" Content="Remove Custom Shiftlock" Icon="Delete16" Command="{Binding RemoveCustomShiftlockModCommand}" Visibility="{Binding DeleteCustomShiftlockVisibility, Mode=OneWay}" />
                  </StackPanel>
                </controls:OptionControl>
                <ui:CardExpander Name="RobloxMenuExpander" Grid.Row="1" Margin="0,8,0,0" IsExpanded="False">
                  <HeaderedContentControl.Header>
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                      </Grid.ColumnDefinitions>
                      <StackPanel Grid.Column="0">
                        <TextBlock FontSize="14" Text="Custom Cursor Set" />
                        <TextBlock Margin="0,2,0,0" FontSize="12" Text="Have multiple cursor sets ready to go for use." Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                      </StackPanel>
                    </Grid>
                  </HeaderedContentControl.Header>
                  <Grid Margin="-8,4,-8,0" MinHeight="300" MaxHeight="500">
                    <Grid.RowDefinitions>
                      <RowDefinition Height="300" />
                      <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="250" />
                      <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <ListBox Name="CustomThemesListBox" Grid.Row="0" Grid.Column="0" Margin="0,0,4,0" DisplayMemberPath="Name" ItemsSource="{Binding CustomCursorSets}" SelectedIndex="{Binding SelectedCustomCursorSetIndex, Mode=TwoWay}" />
                    <Grid Row="1" Column="0" Margin="0,8,4,0">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                      </Grid.ColumnDefinitions>
                      <ui:Button Grid.Column="0" Margin="0,0,4,0" Height="32" VerticalAlignment="Center" Icon="Add28" Content="{x:Static resources:Strings.Common_New}" HorizontalAlignment="Stretch" FontWeight="Medium" Command="{Binding AddCustomCursorSetCommand}" />
                      <ui:Button Grid.Column="1" Margin="4,0,0,0" Height="32" VerticalAlignment="Center" Icon="Delete28" Content="{x:Static resources:Strings.Common_Delete}" HorizontalAlignment="Stretch" FontWeight="Medium" Appearance="Danger" IsEnabled="{Binding IsCustomCursorSetSelected}" Command="{Binding DeleteCustomCursorSetCommand}" />
                    </Grid>
                    <StackPanel Grid.RowSpan="2" Grid.Column="1" Margin="4,0,0,0">
                      <FrameworkElement.Style>
                        <Style TargetType="{x:Type StackPanel}">
                          <Style.Triggers>
                            <DataTrigger Value="False" Binding="{Binding IsCustomCursorSetSelected}">
                              <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                          </Style.Triggers>
                        </Style>
                      </FrameworkElement.Style>
                      <Grid Margin="0,0,0,48">
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="*" />
                          <ColumnDefinition Width="*" />
                          <ColumnDefinition Width="*" />
                          <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                          <RowDefinition Height="Auto" />
                          <RowDefinition Height="16" />
                          <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid Row="0" ColumnSpan="4" Margin="0,16,0,0">
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                          </Grid.ColumnDefinitions>
                          <ui:TextBox Grid.Column="0" PlaceholderText="Custom Cursor Set 1" HorizontalAlignment="Stretch" Text="{Binding SelectedCustomCursorSetName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                          <ui:Button Grid.Column="1" Margin="4,0,0,0" Icon="Edit28" Content="Rename" FontWeight="Medium" Command="{Binding RenameCustomCursorSetCommand}" />
                        </Grid>
                        <Grid Row="2" Column="0" Margin="4,16,8,16">
                          <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                          </Grid.RowDefinitions>
                          <TextBlock Grid.Row="0" Margin="0,0,0,8" Text="ShiftLock" FontSize="14" TextAlignment="Center" />
                          <Border Grid.Row="1" Background="#33000000" Width="96" Height="96" CornerRadius="12" BorderThickness="1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" Margin="0,0,0,8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MaxWidth="200" MaxHeight="200" MinHeight="100" MinWidth="100">
                            <Viewbox Stretch="Uniform">
                              <Image Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Source="{Binding ShiftlockCursorPreview, TargetNullValue={x:Null}}" />
                            </Viewbox>
                          </Border>
                          <StackPanel Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Top" Orientation="Vertical" Margin="0,8,0,0">
                            <ui:Button Content="Browse" Icon="Folder28" HorizontalAlignment="Stretch" Height="32" Command="{Binding AddShiftlockCursorCommand}" Visibility="{Binding AddShiftlockCursorVisibility}" />
                            <ui:Button Content="Remove" Icon="Delete28" Appearance="Danger" HorizontalAlignment="Stretch" Height="32" Command="{Binding DeleteShiftlockCursorCommand}" Visibility="{Binding DeleteShiftlockCursorVisibility}" />
                          </StackPanel>
                        </Grid>
                        <Grid Row="2" Column="1" Margin="8,16,8,16">
                          <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                          </Grid.RowDefinitions>
                          <TextBlock Grid.Row="0" Margin="0,0,0,8" Text="Arrow Cursor" FontSize="14" TextAlignment="Center" />
                          <Border Grid.Row="1" Background="#33000000" Width="96" Height="96" CornerRadius="12" BorderThickness="1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" Margin="0,0,0,8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MaxWidth="200" MaxHeight="200" MinHeight="100" MinWidth="100">
                            <Viewbox Stretch="Uniform">
                              <Image Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Source="{Binding ArrowCursorPreview, TargetNullValue={x:Null}}" />
                            </Viewbox>
                          </Border>
                          <StackPanel Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Top" Orientation="Vertical" Margin="0,8,0,0">
                            <ui:Button Content="Browse" Icon="Folder28" HorizontalAlignment="Stretch" Height="32" Command="{Binding AddArrowCursorCommand}" Visibility="{Binding AddArrowCursorVisibility}" />
                            <ui:Button Content="Remove" Icon="Delete28" Appearance="Danger" HorizontalAlignment="Stretch" Height="32" Command="{Binding DeleteArrowCursorCommand}" Visibility="{Binding DeleteArrowCursorVisibility}" />
                          </StackPanel>
                        </Grid>
                        <Grid Row="2" Column="2" Margin="8,16,8,16">
                          <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                          </Grid.RowDefinitions>
                          <TextBlock Grid.Row="0" Margin="0,0,0,8" Text="ArrowFar Cursor" FontSize="14" TextAlignment="Center" />
                          <Border Grid.Row="1" Background="#33000000" Width="96" Height="96" CornerRadius="12" BorderThickness="1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" Margin="0,0,0,8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MaxWidth="200" MaxHeight="200" MinHeight="100" MinWidth="100">
                            <Viewbox Stretch="Uniform">
                              <Image Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Source="{Binding ArrowFarCursorPreview, TargetNullValue={x:Null}}" />
                            </Viewbox>
                          </Border>
                          <StackPanel Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Top" Orientation="Vertical" Margin="0,8,0,0">
                            <ui:Button Content="Browse" Icon="Folder28" HorizontalAlignment="Stretch" Height="32" Command="{Binding AddArrowFarCursorCommand}" Visibility="{Binding AddArrowFarCursorVisibility}" />
                            <ui:Button Content="Remove" Icon="Delete28" Appearance="Danger" HorizontalAlignment="Stretch" Height="32" Command="{Binding DeleteArrowFarCursorCommand}" Visibility="{Binding DeleteArrowFarCursorVisibility}" />
                          </StackPanel>
                        </Grid>
                        <Grid Row="2" Column="3" Margin="8,16,4,16">
                          <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                          </Grid.RowDefinitions>
                          <TextBlock Grid.Row="0" Margin="0,0,0,8" Text="IBeam Cursor" FontSize="14" TextAlignment="Center" />
                          <Border Grid.Row="1" Background="#33000000" Width="96" Height="96" CornerRadius="12" BorderThickness="1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" Margin="0,0,0,8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MaxWidth="200" MaxHeight="200" MinHeight="100" MinWidth="100">
                            <Viewbox Stretch="Uniform">
                              <Image Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Source="{Binding IBeamCursorPreview, TargetNullValue={x:Null}}" />
                            </Viewbox>
                          </Border>
                          <StackPanel Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Top" Orientation="Vertical" Margin="0,8,0,0">
                            <ui:Button Content="Browse" Icon="Folder28" HorizontalAlignment="Stretch" Height="32" Command="{Binding AddIBeamCursorCommand}" Visibility="{Binding AddIBeamCursorVisibility}" />
                            <ui:Button Content="Remove" Icon="Delete28" Appearance="Danger" HorizontalAlignment="Stretch" Height="32" Command="{Binding DeleteIBeamCursorCommand}" Visibility="{Binding DeleteIBeamCursorVisibility}" />
                          </StackPanel>
                        </Grid>
                      </Grid>
                    </StackPanel>
                    <Grid Row="1" Column="1" Margin="0,8,0,0" Height="36">
                      <FrameworkElement.Style>
                        <Style TargetType="{x:Type Grid}">
                          <Style.Triggers>
                            <DataTrigger Value="False" Binding="{Binding IsCustomCursorSetSelected}">
                              <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                          </Style.Triggers>
                        </Style>
                      </FrameworkElement.Style>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                      </Grid.ColumnDefinitions>
                      <ui:Button Grid.Column="0" Height="32" Content="Apply Set" Icon="Checkmark20" FontWeight="Medium" HorizontalAlignment="Stretch" Margin="4,0,0,0" ToolTip="This will apply your cursors into your Mods Folder." Command="{Binding ApplyCursorSetCommand}" />
                      <ui:Button Grid.Column="1" Height="32" Content="Fetch Set" Icon="CollectionsAdd24" FontWeight="Medium" HorizontalAlignment="Stretch" Margin="4,0,0,0" ToolTip="This will copy the cursors from your Mods folder." Command="{Binding GetCurrentCursorSetCommand}" />
                      <ui:Button Grid.Column="2" Height="32" Content="Import Set" Icon="FolderZip24" FontWeight="Medium" HorizontalAlignment="Stretch" Margin="4,0,0,0" ToolTip="Allows you to Import Cursor from others easily." Command="{Binding ImportCursorSetCommand}" />
                      <ui:Button Grid.Column="3" Height="32" Content="Export Set" Icon="ArrowExportRtl24" FontWeight="Medium" HorizontalAlignment="Stretch" Margin="4,0,0,0" ToolTip="Allows you to export your Cursor to other people or save somewhere." Command="{Binding ExportCursorSetCommand}" />
                    </Grid>
                    <TextBlock Grid.RowSpan="2" Grid.Column="1" Text="No Custom Cursor Set selected." FontWeight="Medium" TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center">
                      <FrameworkElement.Style>
                        <Style TargetType="{x:Type TextBlock}">
                          <Style.Triggers>
                            <DataTrigger Value="True" Binding="{Binding IsCustomCursorSetSelected}">
                              <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                          </Style.Triggers>
                        </Style>
                      </FrameworkElement.Style>
                    </TextBlock>
                  </Grid>
                </ui:CardExpander>
              </StackPanel>
            </ui:CardExpander>
          </StackPanel>
        </TabItem>
        <TabItem FontWeight="Medium">
          <TabItem.Header>
            <StackPanel Orientation="Horizontal">
              <ui:SymbolIcon Symbol="Folder24" Margin="0,0,6,0" />
              <TextBlock Text="Mod Management" />
            </StackPanel>
          </TabItem.Header>
          <StackPanel Margin="12">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <StackPanel>
                <TextBlock FontSize="20" FontWeight="Bold" Text="Mod Management" />
                            </StackPanel>
              <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                <TextBlock HorizontalAlignment="Right" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding ManagedModsSummary}" />
                <ProgressBar Width="120" Height="3" Margin="0,8,0,0" IsIndeterminate="True" Visibility="{Binding ManagedModsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
              </StackPanel>
            </Grid>
            <Grid Margin="0,14,0,12">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="220" />
              </Grid.ColumnDefinitions>
              <ui:Button Appearance="Primary" Icon="Add24" Content="Add Mod" Command="{Binding AddManagedModCommand}" />
              <ui:Button Grid.Column="1" Margin="8,0,0,0" Icon="FolderOpen24" Content="Open Library" Command="{Binding OpenManagedModsRootCommand}" />
              <ui:Button Grid.Column="2" Margin="8,0,0,0" HorizontalAlignment="Left" Icon="ArrowSync24" Content="Refresh" Command="{Binding RefreshManagedModsCommand}" />
              <ui:TextBox Grid.Column="3" PlaceholderText="Search mods or identifiers" Text="{Binding ManagedModSearchText, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
                        <Border Padding="28" CornerRadius="8" BorderThickness="1" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}">
              <Border.Style>
                <Style TargetType="Border">
                  <Setter Property="Visibility" Value="Collapsed" />
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding ManagedMods.Count}" Value="0">
                      <Setter Property="Visibility" Value="Visible" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Border.Style>
              <StackPanel HorizontalAlignment="Center">
                <TextBlock HorizontalAlignment="Center" FontSize="16" FontWeight="SemiBold" Text="{Binding ManagedModsEmptyTitle}" />
                <TextBlock Margin="0,5,0,0" HorizontalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding ManagedModsEmptyDescription}" TextWrapping="Wrap" />
              </StackPanel>
            </Border>
            <ItemsControl x:Name="ManagedModsList" AllowDrop="True" DragOver="ManagedModsList_DragOver" ItemsSource="{Binding ManagedMods}">
              <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type settings:ManagedModItem}">
                  <Border Margin="0,0,0,8" Padding="12" AllowDrop="True" CornerRadius="8" DragEnter="ManagedModCard_DragEnter" DragLeave="ManagedModCard_DragLeave" DragOver="ManagedModCard_DragOver" Drop="ManagedModCard_Drop">
                    <Border.Style>
                      <Style TargetType="Border">
                        <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorDefaultBrush}" />
                        <Setter Property="BorderBrush" Value="{DynamicResource CardStrokeColorDefaultBrush}" />
                        <Setter Property="BorderThickness" Value="1" />
                      </Style>
                    </Border.Style>
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                      </Grid.ColumnDefinitions>
                      <Border Width="30" Height="36" Margin="0,0,6,0" Cursor="SizeAll" AutomationProperties.Name="Drag mod to change apply order" PreviewMouseLeftButtonDown="ManagedModDragHandle_MouseLeftButtonDown" PreviewMouseMove="ManagedModDragHandle_MouseMove">
                        <ui:SymbolIcon Width="20" Height="20" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" Symbol="ReOrderDotsVertical24" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                      </Border>
                      <ToggleButton Grid.Column="1" Width="36" Height="36" Margin="0,0,12,0" Padding="0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" VerticalAlignment="Center" IsChecked="{Binding Enabled, Mode=OneWay}" Command="{Binding DataContext.ToggleManagedModCommand, RelativeSource={RelativeSource AncestorType={x:Type pages:ModsPage}}}" CommandParameter="{Binding}" AutomationProperties.Name="Enable or disable this mod">
                        <ui:SymbolIcon Width="20" Height="20" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" Symbol="Power24" Filled="{Binding Enabled, Mode=OneWay}" />
                      </ToggleButton>
                      <StackPanel Grid.Column="2" Margin="0,0,14,0" VerticalAlignment="Center">
                        <TextBlock FontSize="15" FontWeight="SemiBold" Text="{Binding Name, Mode=OneWay}" />
                        <TextBlock Margin="0,4,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                          <Run Text="{Binding FileSummary, Mode=OneWay}" /><Run Text="   ID: " /><Run Text="{Binding ShortId, Mode=OneWay}" />
                        </TextBlock>
                        <TextBlock Margin="0,4,0,0" Foreground="{DynamicResource SystemFillColorCautionBrush}" Text="{Binding ConflictText}" Visibility="{Binding HasConflicts, Converter={StaticResource BooleanToVisibilityConverter}}" TextWrapping="Wrap" />
                        <TextBlock Margin="0,4,0,0" Foreground="{DynamicResource SystemFillColorCriticalBrush}" Text="{Binding ScanError, Mode=OneWay}" Visibility="{Binding HasScanError, Converter={StaticResource BooleanToVisibilityConverter}}" TextWrapping="Wrap" />
                      </StackPanel>
                      <StackPanel Grid.Column="3" VerticalAlignment="Center" Orientation="Horizontal">
                        <ui:Button Width="36" Height="36" Margin="0,0,6,0" Padding="0" Appearance="Secondary" Icon="FolderOpen24" AutomationProperties.Name="Open mod folder" Command="{Binding DataContext.OpenManagedModCommand, RelativeSource={RelativeSource AncestorType={x:Type pages:ModsPage}}}" CommandParameter="{Binding}" />
                        <ui:Button Width="36" Height="36" Margin="0,0,6,0" Padding="0" Appearance="Secondary" Icon="Edit24" AutomationProperties.Name="Rename mod" Command="{Binding DataContext.RenameManagedModCommand, RelativeSource={RelativeSource AncestorType={x:Type pages:ModsPage}}}" CommandParameter="{Binding}" />
                        <ui:Button Width="36" Height="36" Margin="0,0,6,0" Padding="0" Appearance="Secondary" Icon="Copy24" AutomationProperties.Name="Copy full mod identifier" Command="{Binding DataContext.CopyManagedModIdCommand, RelativeSource={RelativeSource AncestorType={x:Type pages:ModsPage}}}" CommandParameter="{Binding}" />
                        <ui:Button Width="36" Height="36" Padding="0" Appearance="Danger" Icon="Delete24" AutomationProperties.Name="Remove mod" Command="{Binding DataContext.RemoveManagedModCommand, RelativeSource={RelativeSource AncestorType={x:Type pages:ModsPage}}}" CommandParameter="{Binding}" />
                      </StackPanel>
                    </Grid>
                  </Border>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </StackPanel>
        </TabItem>
        <TabItem FontWeight="Medium" Visibility="{x:Static platform:PlatformFeatureVisibility.Overlay}">
          <TabItem.Header>
            <StackPanel Orientation="Horizontal">
              <ui:SymbolIcon Symbol="Layer24" Margin="0,0,6,0" />
              <TextBlock Text="Overlays" />
            </StackPanel>
          </TabItem.Header>
          <StackPanel Margin="12,12,12,12" Height="Auto">
            <TextBlock FontSize="20" FontWeight="Bold" Margin="0,0,0,8" Text="Overlays" />
            <controls:OptionControl Header="Enable Overlay" Description="Enables the Overlay Mods to work over Roblox.">
              <ui:ToggleSwitch IsChecked="{Binding OverlaysEnabled, Mode=TwoWay}" />
            </controls:OptionControl>
                        <controls:OptionControl Header="Homepage Background" Description="Change you're Roblox Home">
              <StackPanel Width="360">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>
                  <ComboBox Width="190" HorizontalAlignment="Left" Padding="10,5" ItemsSource="{Binding HomepageBackgroundModes}" SelectedItem="{Binding SelectedHomepageBackgroundMode, Mode=TwoWay}" />
                  <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <ui:ToggleSwitch IsChecked="{Binding HomepageBackgroundOverlayEnabled, Mode=TwoWay}" />
                  </StackPanel>
                </Grid>
                <StackPanel Margin="0,10,0,0" Visibility="{Binding ShowHomepageSolidColor, Converter={StaticResource BooleanToVisibilityConverter}}">
                  <TextBlock Margin="0,0,0,5" Text="Background color" />
                  <Button Width="120" HorizontalAlignment="Left" Content="{Binding HomepageBackgroundOverlayColor}" Command="{Binding PickHomepageBackgroundColorCommand}" />
                </StackPanel>
                <StackPanel Margin="0,10,0,0" Visibility="{Binding ShowHomepageGradient, Converter={StaticResource BooleanToVisibilityConverter}}">
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Margin="0,0,6,0">
                      <TextBlock Margin="0,0,0,5" Text="Start color" />
                      <Button Content="{Binding HomepageBackgroundOverlayColor}" Command="{Binding PickHomepageBackgroundColorCommand}" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="6,0,0,0">
                      <TextBlock Margin="0,0,0,5" Text="End color" />
                      <Button Content="{Binding HomepageBackgroundOverlayGradientColor}" Command="{Binding PickHomepageBackgroundGradientColorCommand}" />
                    </StackPanel>
                  </Grid>
                  <TextBlock Margin="0,10,0,4" Text="Direction" />
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="48" />
                    </Grid.ColumnDefinitions>
                    <Slider Minimum="0" Maximum="360" TickFrequency="1" Value="{Binding HomepageBackgroundOverlayGradientAngle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" Text="{Binding HomepageBackgroundOverlayGradientAngleDisplay}" />
                  </Grid>
                </StackPanel>
                <StackPanel Margin="0,10,0,0" Visibility="{Binding ShowHomepageMedia, Converter={StaticResource BooleanToVisibilityConverter}}">
                  <TextBlock Margin="0,0,0,5" Text="Image, GIF, or video" />
                  <Border Height="150" Margin="0,0,0,8" CornerRadius="6"
                          Background="{DynamicResource ControlFillColorSecondaryBrush}"
                          BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1"
                          ClipToBounds="True" SnapsToDevicePixels="True"
                          Visibility="{Binding HasHomepageBackgroundMedia, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid>
                      <Image Stretch="UniformToFill" IsHitTestVisible="False"
                             Source="{Binding HomepageMediaStillSource}"
                             Visibility="{Binding HomepageMediaIsStill, Converter={StaticResource BooleanToVisibilityConverter}}" />
                      <Image Stretch="UniformToFill" IsHitTestVisible="False"
                             gif:ImageBehavior.RepeatBehavior="Forever"
                             gif:ImageBehavior.AnimatedSource="{Binding HomepageMediaAnimatedSource}"
                             Visibility="{Binding HomepageMediaIsAnimated, Converter={StaticResource BooleanToVisibilityConverter}}" />
                      <MediaElement Stretch="UniformToFill" IsHitTestVisible="False"
                                    LoadedBehavior="Play" UnloadedBehavior="Close" Volume="0"
                                    Source="{Binding HomepageMediaPreviewVideoUri}"
                                    MediaEnded="HomepageMediaPreviewVideo_MediaEnded"
                                    Visibility="{Binding HomepageMediaIsVideo, Converter={StaticResource BooleanToVisibilityConverter}}" />
                      <TextBlock Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap"
                                 TextAlignment="Center" Opacity="0.7" Text="{Binding HomepageMediaPreviewMessage}"
                                 Visibility="{Binding HasHomepageMediaPreviewMessage, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    </Grid>
                  </Border>
                  <ui:Button MinWidth="130" HorizontalAlignment="Left"
                             Content="{Binding HomepageBackgroundMediaButtonText}"
                             Command="{Binding ToggleHomepageBackgroundMediaCommand}">
                    <FrameworkElement.Style>
                      <Style BasedOn="{StaticResource {x:Type ui:Button}}" TargetType="{x:Type ui:Button}">
                        <Style.Triggers>
                          <DataTrigger Binding="{Binding HasHomepageBackgroundMedia}" Value="True">
                            <Setter Property="Appearance" Value="Danger" />
                            <Setter Property="Background" Value="#FF3334" />
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </FrameworkElement.Style>
                  </ui:Button>
                  <TextBlock Margin="0,6,0,0" Opacity="0.7" Text="{Binding HomepageBackgroundOverlayMediaName}" TextTrimming="CharacterEllipsis" />
                </StackPanel>
              </StackPanel>
            </controls:OptionControl>
            <controls:OptionControl Header="Clock" Description="Displays a clock in the stats overlay.">
              <ui:ToggleSwitch IsChecked="{Binding CurrentTimeDisplay, Mode=TwoWay}" />
            </controls:OptionControl>
            <StackPanel IsEnabled="{Binding CurrentTimeDisplay}">
              <controls:OptionControl Header="Clock Time Zone" Description="Pick which time zone the clock shows. System time zone follows this computer.">
                <ComboBox Width="280" Padding="10,5,10,5" SelectedValuePath="Id" DisplayMemberPath="Display"
                          ItemsSource="{Binding ClockTimeZones, Mode=OneTime}"
                          SelectedValue="{Binding ClockTimeZoneId, Mode=TwoWay}" />
              </controls:OptionControl>
              <controls:OptionControl Header="24 Hour Clock" Description="Shows the clock as 14:30 instead of 2:30 PM.">
                <ui:ToggleSwitch IsChecked="{Binding Clock24Hour, Mode=TwoWay}" />
              </controls:OptionControl>
            </StackPanel>
            <controls:OptionControl Header="Brightness" Description="Adjust the screen brightness level">
              <StackPanel>
                <Slider Minimum="0" Maximum="100" TickFrequency="5" IsSnapToTickEnabled="True" Value="{Binding Brightness, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" Text="{Binding BrightnessDisplay}" />
              </StackPanel>
            </controls:OptionControl>
            <controls:OptionControl Header="Saturation" Description="Adjust color saturation. 100 is default." Visibility="{x:Static platform:PlatformFeatureVisibility.WindowsIntegration}">
              <StackPanel>
                <Slider Minimum="0" Maximum="200" TickFrequency="5" IsSnapToTickEnabled="True" Value="{Binding Saturation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Right" Text="{Binding SaturationDisplay}" />
              </StackPanel>
            </controls:OptionControl>
            <controls:OptionControl Header="Contrast" Description="Adjust contrast level. 100 is default." Visibility="{x:Static platform:PlatformFeatureVisibility.WindowsIntegration}">
              <StackPanel>
                <Slider Minimum="0" Maximum="200" TickFrequency="5" IsSnapToTickEnabled="True" Value="{Binding Contrast, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Right" Text="{Binding ContrastDisplay}" />
              </StackPanel>
            </controls:OptionControl>
                        <controls:OptionControl Header="Color Temperature" Description="Warm or cool color tint. 0 is neutral." Visibility="{x:Static platform:PlatformFeatureVisibility.WindowsIntegration}">
                            <StackPanel>
                                <Slider Minimum="-100" Maximum="100" TickFrequency="5" IsSnapToTickEnabled="True" Value="{Binding ColorTemperature, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Right" Text="{Binding ColorTemperatureDisplay}" />
                            </StackPanel>
                        </controls:OptionControl>

            <controls:OptionControl Header="Anti Aliasing" Description="Smooths jagged edges over Roblox with your GPU" Visibility="{x:Static platform:PlatformFeatureVisibility.WindowsIntegration}">
              <ComboBox Padding="10,5,10,5" MinWidth="140" ItemsSource="{Binding AntiAliasingMethodNames, Mode=OneTime}" SelectedIndex="{Binding AntiAliasingMethodIndex, Mode=TwoWay}" />
            </controls:OptionControl>
            <controls:OptionControl Header="Frame Generation" Description="Auto generates as many stable frames as your game, Adds a little input latency" Visibility="{x:Static platform:PlatformFeatureVisibility.FrameGeneration}">
              <ui:ToggleSwitch IsChecked="{Binding FrameGenEnabled, Mode=TwoWay}" />
            </controls:OptionControl>
            <controls:OptionControl Header="Generated Frames Overlay" Description="Displays how many frames per second are being generated" Visibility="{x:Static platform:PlatformFeatureVisibility.FrameGeneration}">
              <ui:ToggleSwitch IsChecked="{Binding FrameGenOverlayShow, Mode=TwoWay}" />
            </controls:OptionControl>
            <ui:InfoBar Margin="0,4,0,0" Severity="Warning" IsClosable="False" IsOpen="{Binding FrameGenCapWarningOpen, Mode=OneWay}" Message="{Binding FrameGenCapWarningMessage, Mode=OneWay}">
              <Button Content="Fix FrameCap" Command="{Binding FixFrameCapCommand}" />
            </ui:InfoBar>
                        <ui:CardExpander IsExpanded="False" Margin="0,4,0,0" Header="Color Blindness Correction">
                            <StackPanel>
                                <controls:OptionControl Header="Enable Correction" Description="Recolor the screen so color blind people can see the original colors" Visibility="{x:Static platform:PlatformFeatureVisibility.WindowsIntegration}">
                                    <ui:ToggleSwitch IsChecked="{Binding ColorBlindnessEnabled, Mode=TwoWay}" />
                                </controls:OptionControl>
                                <controls:OptionControl Header="Correction Type" Description="Select your type of color blindness" Visibility="{x:Static platform:PlatformFeatureVisibility.WindowsIntegration}">
                                    <ComboBox SelectedIndex="{Binding ColorBlindnessType, Mode=TwoWay}">
                                        <ComboBoxItem Content="Protanopia (red/green)" />
                                        <ComboBoxItem Content="Deuteranopia (red/green)" />
                                        <ComboBoxItem Content="Tritanopia (blue/yellow)" />
                                    </ComboBox>
                                </controls:OptionControl>
                                <controls:OptionControl Header="Severity" Description="How strong the correction should be. 100 is full dichromacy correction" Visibility="{x:Static platform:PlatformFeatureVisibility.WindowsIntegration}">
                                    <StackPanel>
                                        <Slider Minimum="0" Maximum="100" TickFrequency="25" IsSnapToTickEnabled="True"
                            Value="{Binding ColorBlindnessSeverity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Right" Text="{Binding ColorBlindnessSeverityDisplay}" />
                                    </StackPanel>
                                </controls:OptionControl>
                                <controls:OptionControl Header="Simulate Mode" Description="Simulate what color blind people see (instead of correcting)" Visibility="{x:Static platform:PlatformFeatureVisibility.WindowsIntegration}">
                                    <ui:ToggleSwitch IsChecked="{Binding ColorBlindnessSimulate, Mode=TwoWay}" />
                                </controls:OptionControl>
                            </StackPanel>
                        </ui:CardExpander>
                        <ui:CardExpander IsExpanded="False" Margin="0,8,0,0" Header="Custom Crosshair">
              <StackPanel>
                <controls:OptionControl Header="Crosshair">
                  <ui:ToggleSwitch IsChecked="{Binding Crosshair, Mode=TwoWay}" />
                </controls:OptionControl>
                <controls:OptionControl Header="Cursor Shape" Description="Select crosshair shape.">
                  <ComboBox Width="160" ItemsSource="{Binding CrosshairShapes}" SelectedItem="{Binding SelectedShape, Mode=TwoWay}" />
                </controls:OptionControl>
                <controls:OptionControl Header="GIF Crosshair" Description="Use an animated GIF URL as your crosshair." Visibility="{Binding SelectedShape, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Image}">
                  <StackPanel>
                    <TextBox Width="260" Text="{Binding ImageUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                  </StackPanel>
                </controls:OptionControl>
                <controls:OptionControl Header="Cursor Color" Description="Set the main cursor color (HEX)." Visibility="{Binding SelectedShape, Converter={StaticResource EnumNotEqualsVisibilityConverter}, ConverterParameter=Image}">
                  <StackPanel Orientation="Horizontal" Margin="0,2">
                    <TextBox Width="120" Text="{Binding CursorColorHex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <ui:Button Content="Pick" Margin="4,0,0,0" Command="{Binding PickCursorColorCommand}" />
                  </StackPanel>
                </controls:OptionControl>
                <controls:OptionControl Header="Cursor Outline" Description="Set the cursor outline color (HEX)." Visibility="{Binding SelectedShape, Converter={StaticResource EnumNotEqualsVisibilityConverter}, ConverterParameter=Image}">
                  <StackPanel Orientation="Horizontal" Margin="0,2">
                    <TextBox Width="120" Text="{Binding CursorOutlineColorHex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <ui:Button Content="Pick" Margin="4,0,0,0" Command="{Binding PickOutlineColorCommand}" />
                  </StackPanel>
                </controls:OptionControl>
                <controls:OptionControl Header="Cursor Size" Description="Adjust the cursor size.">
                  <StackPanel Orientation="Horizontal" Margin="0,2">
                    <Slider Width="200" Minimum="4" Maximum="64" Value="{Binding CursorSize, Mode=TwoWay}" />
                    <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" Text="{Binding CursorSize}" />
                  </StackPanel>
                </controls:OptionControl>
                <controls:OptionControl Header="Line Thickness" Description="Adjust crosshair thickness." Visibility="{Binding SelectedShape, Converter={StaticResource EnumNotEqualsVisibilityConverter}, ConverterParameter=Image}">
                  <StackPanel Orientation="Horizontal" Margin="0,2">
                    <Slider Width="200" Minimum="1" Maximum="10" Value="{Binding CrosshairThickness, Mode=TwoWay}" />
                    <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" Text="{Binding CrosshairThickness}" />
                  </StackPanel>
                </controls:OptionControl>
                <controls:OptionControl Header="Center Gap" Description="Gap from the center." Visibility="{Binding SelectedShape, Converter={StaticResource EnumNotEqualsVisibilityConverter}, ConverterParameter=Image}">
                  <StackPanel Orientation="Horizontal" Margin="0,2">
                    <Slider Width="200" Minimum="0" Maximum="32" Value="{Binding Gap, Mode=TwoWay}" />
                    <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" Text="{Binding Gap}" />
                  </StackPanel>
                </controls:OptionControl>
                <controls:OptionControl Header="Opacity" Description="Adjust crosshair opacity.">
                  <StackPanel Orientation="Horizontal" Margin="0,2">
                    <Slider Width="200" Minimum="0.1" Maximum="1.0" Value="{Binding CursorOpacity, Mode=TwoWay}" />
                    <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" Text="{Binding CursorOpacity}" />
                  </StackPanel>
                </controls:OptionControl>
                <controls:OptionControl Header="Preview" Description="Live preview of your crosshair.">
                  <Border Width="208" Height="208" BorderThickness="1" BorderBrush="#FF808080" CornerRadius="4">
                    <Image Stretch="Uniform" Source="{Binding CursorPreview, TargetNullValue={x:Null}}" />
                  </Border>
                </controls:OptionControl>
                <controls:OptionControl Header="Cursor Code" Description="Share or import crosshair codes.">
                  <StackPanel Orientation="Horizontal" Margin="0,2">
                    <TextBox Width="220" Text="{Binding CursorCode, Mode=TwoWay}" />
                    <ui:Button Content="Generate" Margin="4,0,0,0" Command="{Binding GenerateCursorCodeCommand}" />
                    <ui:Button Content="Apply" Margin="4,0,0,0" Command="{Binding ApplyCursorCodeCommand}" />
                  </StackPanel>
                </controls:OptionControl>
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                  <Button Content="Download .CUR" Margin="0,0,5,0" Height="35" Command="{Binding DownloadCurCommand}" />
                  <Button Content="Download .PNG" Height="35" Command="{Binding DownloadPngCommand}" />
                </StackPanel>
              </StackPanel>
            </ui:CardExpander>
          </StackPanel>
        </TabItem>
      </TabControl>
    </StackPanel>
    <Grid Margin="0,0,0,0" Visibility="{Binding ModExplorerVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <Grid Row="0" Margin="0,0,0,12">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal">
          <ui:Button Content="Refresh" Icon="ArrowClockwise24" Appearance="Primary" Margin="0,0,8,0" Command="{Binding RefreshModFilesCommand}" />
          <ui:Button Content="Delete" Icon="Delete24" Appearance="Danger" Margin="0,0,8,0" Command="{Binding DeleteModFileCommand}" />
          <ui:Button Content="Open Folder" Icon="FolderOpen24" Appearance="Secondary" Margin="0,0,16,0" Command="{Binding OpenModFileFolderCommand}" />
        </StackPanel>
      </Grid>
      <Grid Row="1" Margin="0,0,0,12">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal">
          <ui:Button Icon="ArrowLeft24" Padding="8,4" Margin="0,0,8,0" ToolTip="Go Back" Command="{Binding GoBackCommand}" />
          <TextBlock Text="Roblox" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
          <TextBlock Text=" &gt; " VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
          <TextBlock VerticalAlignment="Center" FontWeight="Medium" Text="{Binding ExplorerPathDisplay}" />
        </StackPanel>
        <ui:TextBox Grid.Row="1" Grid.Column="1" Width="300" PlaceholderText="Search assets (e.g. mesh, texture, sound)" Icon="Search24" Text="{Binding ExplorerSearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
      </Grid>
      <Grid Row="2" Height="640" MinHeight="320">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" MinWidth="300" />
          <ColumnDefinition Width="14" />
          <ColumnDefinition Width="400" MinWidth="300" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Margin="24" HorizontalAlignment="Center" VerticalAlignment="Center"
                   TextWrapping="Wrap" TextAlignment="Center" FontSize="13" MaxWidth="360"
                   Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                   Text="{Binding ExplorerStatusMessage}"
                   Visibility="{Binding ExplorerHasStatus, Converter={StaticResource BooleanToVisibilityConverter}}" />
        <DataGrid Name="ModGrid" Grid.Column="0" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserReorderColumns="False" CanUserResizeColumns="True" CanUserSortColumns="True" SelectionMode="Single" HeadersVisibility="Column" GridLinesVisibility="Horizontal" Background="#00FFFFFF" BorderThickness="0" Foreground="{DynamicResource TextFillColorPrimaryBrush}" RowBackground="#00FFFFFF" HorizontalGridLinesBrush="{DynamicResource ControlElevationBorderBrush}" ItemsSource="{Binding ModFiles}" SelectedItem="{Binding SelectedModFile}" SelectionChanged="ModGrid_SelectionChanged" MouseDoubleClick="ModGrid_MouseDoubleClick"
                  EnableRowVirtualization="True" EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  VirtualizingPanel.ScrollUnit="Item"
                  ScrollViewer.CanContentScroll="True"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
          <DataGrid.Columns>
            <DataGridTemplateColumn Header="Name" Width="*">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Margin="8,0,8,0" Symbol="{Binding Icon}" />
                    <TextBlock VerticalAlignment="Center" Text="{Binding Name}" />
                  </StackPanel>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTextColumn Header="Type" Width="45" Binding="{Binding Type}" />
            <DataGridTemplateColumn Header="Status" Width="75">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock VerticalAlignment="Center" Margin="4,0" FontWeight="SemiBold" Text="{Binding Status}">
                    <FrameworkElement.Style>
                      <Style TargetType="{x:Type TextBlock}">
                        <Style.Triggers>
                          <DataTrigger Value="Success" Binding="{Binding StatusColor}">
                            <Setter Property="Foreground" Value="#4CAF50" />
                          </DataTrigger>
                          <DataTrigger Value="Warning" Binding="{Binding StatusColor}">
                            <Setter Property="Foreground" Value="#FFC107" />
                          </DataTrigger>
                          <DataTrigger Value="Danger" Binding="{Binding StatusColor}">
                            <Setter Property="Foreground" Value="#F44336" />
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </FrameworkElement.Style>
                  </TextBlock>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTextColumn Header="Size" Width="70" Binding="{Binding SizeText}" />
            <DataGridTextColumn Header="Modified" Width="70" Binding="{Binding ModifiedTime}" />
          </DataGrid.Columns>
        </DataGrid>
        <GridSplitter Grid.Column="1" Width="14" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#00FFFFFF" ResizeBehavior="PreviousAndNext" ResizeDirection="Columns">
          <Control.Template>
            <ControlTemplate TargetType="{x:Type GridSplitter}">
              <Grid Background="#00FFFFFF">
                <Border Width="6" Height="44" CornerRadius="3" VerticalAlignment="Center" Background="{DynamicResource ControlFillColorSecondaryBrush}" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1">
                  <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Ellipse Width="3" Height="3" Margin="0,1.5" Fill="{DynamicResource TextFillColorTertiaryBrush}" />
                    <Ellipse Width="3" Height="3" Margin="0,1.5" Fill="{DynamicResource TextFillColorTertiaryBrush}" />
                    <Ellipse Width="3" Height="3" Margin="0,1.5" Fill="{DynamicResource TextFillColorTertiaryBrush}" />
                  </StackPanel>
                </Border>
              </Grid>
            </ControlTemplate>
          </Control.Template>
        </GridSplitter>
        <Grid Column="2">
          <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <controls:AssetPreviewPanel x:Name="ModPreviewPanel" Grid.Row="0" />
          <Border Grid.Row="1" Margin="0,8,0,0" Padding="8" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="3" BorderThickness="1" BorderBrush="{DynamicResource ControlElevationBorderBrush}">
            <StackPanel>
              <TextBlock Text="File Operations" FontWeight="Bold" Margin="0,0,0,8" />
              <WrapPanel>
                <FrameworkElement.Resources>
                  <ResourceDictionary>
                    <Style x:Key="{x:Type ui:Button}" TargetType="{x:Type ui:Button}" BasedOn="{StaticResource {x:Type ui:Button}}">
                      <Setter Property="Padding" Value="8,4" />
                      <Setter Property="FontSize" Value="11" />
                      <Setter Property="Margin" Value="0,0,4,4" />
                    </Style>
                  </ResourceDictionary>
                </FrameworkElement.Resources>
                <ui:Button Content="Details" Icon="Info24" Command="{Binding ShowFileDetailsCommand}" />
                <ui:Button Content="Replace" Icon="ArrowSwap24" Appearance="Primary" ToolTip="Select a local file to replace this asset in your Mods folder." Command="{Binding ReplaceFileCommand}" />
                <ui:Button Content="Readjust" Icon="Edit24" ToolTip="Rotate, Flip, or Resize this image." Command="{Binding AdjustImageCommand}" />
                <ui:Button Content="Recolor" Icon="Color24" ToolTip="Tint this image a new color." Command="{Binding RecolorImageCommand}" Visibility="{Binding SelectedModFile.IsImage, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <ui:Button Content="Export" Icon="ArrowDownload24" ToolTip="Save this file to your computer." Command="{Binding ExportFileCommand}" />
              </WrapPanel>
            </StackPanel>
          </Border>
        </Grid>
      </Grid>
    </Grid>
  </StackPanel>
</ui:UiPage>
