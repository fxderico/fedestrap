<base:WpfUiWindow x:Class="Fedestrap.UI.Elements.Bootstrapper.FluentDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:base="clr-namespace:Fedestrap.UI.Elements.Base" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:resources="clr-namespace:Fedestrap.Resources" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" xmlns:vms="clr-namespace:Fedestrap.UI.ViewModels.Bootstrapper" Closing="UiWindow_Closing" Width="500" Height="280" MinHeight="0" ExtendsContentIntoTitleBar="True" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="None" xmlns:bootstrapper="clr-namespace:Fedestrap.UI.Elements.Bootstrapper" Background="{ui:ThemeResource ApplicationBackgroundBrush}">
  <Window.TaskbarItemInfo>
    <TaskbarItemInfo ProgressState="{Binding TaskbarProgressState}" ProgressValue="{Binding TaskbarProgressValue}" />
  </Window.TaskbarItemInfo>
  <Grid>
    <Image Name="BackgroundImage" Stretch="Fill" OpacityMask="#FF000000" Panel.ZIndex="-1" />
    <Rectangle Name="GlassTint" Margin="-320,-320,-320,-320" Fill="{Binding BackgroundColourBrush, Mode=OneWay}">
      <UIElement.RenderTransform>
        <TranslateTransform x:Name="GlassTintTransform" />
      </UIElement.RenderTransform>
    </Rectangle>
    <ui:TitleBar VerticalAlignment="Top" CanMaximize="False" ShowClose="False" ShowMaximize="False" ShowMinimize="False" />
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="12,8,0,0" Panel.ZIndex="10" Visibility="{Binding ProfileVisibility, Mode=OneWay}">
      <Grid Width="56" Height="56" VerticalAlignment="Center">
        <Image Source="{Binding BorderImage, Mode=OneWay}" Width="{Binding BorderImageWidth, Mode=OneWay}" Height="{Binding BorderImageHeight, Mode=OneWay}" Margin="{Binding BorderImageMargin, Mode=OneWay}" HorizontalAlignment="Left" VerticalAlignment="Top" Stretch="Fill" Panel.ZIndex="{Binding BorderImageZIndex, Mode=OneWay}" Visibility="{Binding BorderImageVisibility, Mode=OneWay}" RenderOptions.BitmapScalingMode="HighQuality" />
        <Grid Width="34" Height="34" HorizontalAlignment="Center" VerticalAlignment="Center">
          <Ellipse Margin="3" Fill="#40000000" />
          <Ellipse Margin="3">
            <Ellipse.Fill>
              <ImageBrush ImageSource="{Binding ProfileAvatar, Mode=OneWay}" Stretch="UniformToFill" />
            </Ellipse.Fill>
          </Ellipse>
          <Ellipse Stroke="{Binding ProfileBorderBrush, Mode=OneWay}" StrokeThickness="2" />
        </Grid>
      </Grid>
      <TextBlock Text="{Binding ProfileName, Mode=OneWay}" VerticalAlignment="Center" Margin="4,0,0,0" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" MaxWidth="180" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
    </StackPanel>
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
      <StackPanel.LayoutTransform>
        <ScaleTransform x:Name="ContentScale" ScaleX="1" ScaleY="1" />
      </StackPanel.LayoutTransform>
      <Image Width="82" Height="82" Margin="0,0,0,14" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality" Source="{Binding Icon, Mode=OneWay, TargetNullValue={x:Null}, FallbackValue={x:Null}}" />
      <TextBlock HorizontalAlignment="Center" FontSize="16" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextWrapping="Wrap" TextAlignment="Center" Text="{Binding Message, Mode=OneWay, Converter={StaticResource StatusLineConverter}}" />
      <TextBlock Margin="0,5,0,0" HorizontalAlignment="Center" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" TextAlignment="Center" Text="{Binding Message, Mode=OneWay, Converter={StaticResource StatusLineConverter}, ConverterParameter=rest}">
        <TextBlock.Style>
          <Style TargetType="TextBlock">
            <Setter Property="Visibility" Value="Visible" />
            <Style.Triggers>
              <DataTrigger Binding="{Binding Message, Mode=OneWay, Converter={StaticResource StatusLineConverter}, ConverterParameter=rest}" Value="">
                <Setter Property="Visibility" Value="Collapsed" />
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </TextBlock.Style>
      </TextBlock>
      <ProgressBar Width="420" Height="13" Margin="0,20,0,16" IsIndeterminate="{Binding ProgressIndeterminate, Mode=OneWay}" Maximum="{Binding ProgressMaximum, Mode=OneWay}" Value="{Binding ProgressValue, Mode=OneWay}" />
      <Button  Width="200" Padding="5" HorizontalAlignment="Center" FontSize="15" Content="{x:Static resources:Strings.Common_Cancel}" Command="{Binding CancelInstallCommand}" IsEnabled="{Binding CancelEnabled, Mode=OneWay}" />
    </StackPanel>
  </Grid>
</base:WpfUiWindow>